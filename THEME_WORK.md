# 鼎恒餐饮 · 商家端 / 供应商端 外观优化 进度文件

> **用法**：新开对话窗口时，第一句发：
> `先读项目根目录的 HANDOFF.md 和 THEME_WORK.md，按「当前唯一任务」执行。动手前先做回退快照。`
> 每完成一项改动，**当场**更新 `HANDOFF.md` 与文末「进度记录」——记忆靠文件，不靠窗口。

---

## 一、项目背景

- 项目根目录：`dingheng-canyin`
- 本地服务：`localhost:3000`
- 产品定位：餐饮供应链平台，核心是采购，附带扫码点餐，服务中小餐饮门店
- 三个端：`dev-console.html`（开发者端，已验收合格，是基线）、
  `public/admin.html`（商家端）、`supplier-dashboard.html`（供应商端）
- 目标：商家端和供应商端外观**对齐开发者端，且要更好、更实用、更用心**
  （这两个端比开发者端更重要）

---

## 二、硬性约束（违反即返工）

1. **经典版 classic 保持不变**（布局、间距、字号一律不动，一个像素都不改）
2. **禁止**：emoji 图标、彩色圆形图标块、装饰色条、渐变
3. **四句主题文案一字不得改动**：
   - 经典版：走了很远，也别忘了出发的样子。
   - 暖橙：人间烟火气，最抚凡人心。
   - 深色：夜深了，数字还在替你守着生意。
   - 青绿：新鲜，是这行最贵的品质。
4. **四套主题不能只是换颜色**——结构、间距、圆角、卡片形态都要有区别，
   是四套不同的设计，不是同一张皮换四种色
5. **每次只改一类，改完等确认，不要一次性大改**

---

## 三、已确认的诊断结论（不要重复诊断，直接采信）

### ✅ 已验证为真

| # | 结论 | 位置 |
|---|---|---|
| 1 | `theme.css` 被 admin.html 正确加载，路径没问题 | `admin.html:7` |
| 2 | **供应商端完全没引入 theme.css**，全文 0 处 theme 相关，自带 12 个老令牌的 `:root`，零主题支持 | `supplier-dashboard.html` |
| 3 | admin 内联 `:root` 覆盖 `--nav / --nav-2`，导致 classic 与 warm 侧栏完全同色 | `admin.html:12-13` |
| 4 | fresh（青绿）侧栏底色是纯白 `#FFFFFF` | `theme.css:451` |
| 5 | 侧栏三个子元素写死白色系，白底上看不见：`.sidebar .brand` `color:#fff`；`.sidebar .logout` `color:#fff` + `background:rgba(255,255,255,.08)`；`.nav-tool` `color:var(--slate-400)` | `admin.html:64`、`105-106`、`124` |
| 6 | `.main` 无 `margin:0 auto`，宽屏右侧空一大块（开发者端是居中的，所以不违和） | `admin.html:134` |
| 7 | 侧栏 22 个菜单项超出一屏，退出登录按钮被挤出首屏，必须滚动才能看到 | `.sidebar` 结构 |
| 8 | `theme-fix.css`、`theme-min.css` 无任何页面引用 | — |

### ❌ 已证伪（不要再往这个方向改）

- ~~工作台 SVG 图标缺样式导致变黑块~~：**假的**。
  `theme.css:1273-1280` 已正确设置 `fill:none; stroke:currentColor; stroke-width:1.5`。
  **真正的问题**：用户看到的「方块」是 **emoji 缺字体渲染失败**（显示成 □），
  根因是 emoji 本身，不是 SVG。
- ~~theme.css 少一个花括号导致文件损坏~~：**假的**，CSS 文件末尾会自动闭合。

### 代码规模参考

| 文件 | 硬编码 hex | rgba | 渐变 |
|---|---|---|---|
| admin.html | 70 | 79 | 50 |
| supplier-dashboard.html | 506 | 58 | 38 |
| dev-console.html（合格基线） | 35 | 20 | 3 |

---

## 四、已拍板的两个决策

**决策 1：emoji 全部换成线性 SVG，四套主题都换，包含 classic。**
- 理由：「禁止 emoji」是硬约束；且 emoji 在缺字体的环境渲染成 □，属于故障，
  「classic 一像素不变」的本意是别改坏，不是把故障冻结住。
- 硬条件：新图标**外框尺寸、位置、间距必须与原 emoji 完全一致**，只换内容不换占位，
  保证布局零位移。
- 风格：1.5px 描边、`fill:none`、`stroke:currentColor`、无底色、无彩色圆块。
- 换完必须逐主题截图核对。

**决策 2：warm（暖橙）侧栏允许与 classic 拉开差异，classic 保持不变。**

---

## 五、当前任务：第 1 类（先做这个）

> ⚠️ **范围严格限定为以下三项，不要顺手加顶栏、不要做别的。**
> 加顶栏是结构性大改，之前正是这类动作改出了「重叠」，留到后面单独决定。

### 第 0 步（动手前必须完成，未完成不得开始改）

```bash
git add -A
git commit -m "改版前快照"
```
外加复制 `admin.html.bak`、`supplier-dashboard.html.bak`。

### 1. 修复 fresh 青绿下「白底白字」
把以下三处改为读取主题令牌，使白色侧栏下文字可见：
- `admin.html:64` `.sidebar .brand { color: #fff }`
- `admin.html:105-106` `.sidebar .logout { background: rgba(255,255,255,.08); color: #fff }`
- `admin.html:124` `.nav-tool { color: var(--slate-400) }`

### 2. 退出登录按钮置底常驻
侧栏 22 项超出一屏，退出登录被挤出首屏（**这是用户此前「退不回登录页」的真正原因**）。
改为 **sticky 固定在侧栏底部**，任何滚动位置都能看到。

### 3. 内容区居中
`.main` 当前 `margin-left:240px; width:calc(100% - 240px)`，无 `margin:0 auto`。
改为居中，参照开发者端 `dev-console.html` 的 `max-width:1100px; margin:0 auto`。

### 改完必须回报
明确列出：**改了哪几行、改前是什么、改后是什么**。等验证通过再进入第 2 类。

---

## 六、后续执行顺序（第 1 类验证通过后）

| 顺序 | 任务 | 说明 |
|---|---|---|
| 第 2 类 | 供应商端接通主题系统 | 引入 theme.css、删除内联 `:root` 的 12 个老令牌、加主题入口。从零接入，工作量大但风险低 |
| 第 3 类 | 四套主题的结构差异 | 侧栏、统计卡、表格、按钮的形态差异（不只是换色），含 warm 侧栏与 classic 拉开差异 |
| 第 4 类 | 清理写死色与 emoji | admin.html 的 70 处 hex / 79 处 rgba / 50 处渐变；全部 emoji 换线性 SVG |

---

## 七、安全红线

1. **`public/theme-fix.css` 和 `public/theme-min.css` 绝对不要接入任何页面。**
   它们是历史遗留的 `!important` 强制补丁（当年越改越乱的产物），
   接进来会再次覆盖新样式，重蹈覆辙。
2. **每次只改一类，改完等确认。**
3. **未做回退快照不得开始改。**
4. **供应商端已有一个 `fmtMoney(n)`（表格在用，`toFixed(2)`，不折算，被 7 处表格调用）。**
   表格要的是精确金额，**绝不能折算成「万」**；统计卡用的是另起的 `fmtBigMoney()`。
   新增格式化函数前务必先查重名 —— 同名函数声明会被后定义的静默覆盖，很难查。

---

## 八、省上下文的工作方式（重要）

本项目文件很大（`admin.html` 近 6000 行），全文读取会迅速耗尽上下文。请遵守：

1. **按需按行号范围读文件**，不要整文件读。例如：`读 admin.html 第 60-135 行`。
2. **不要重复做已完成的诊断**，直接采信本文档第三节。
3. **每完成一类改动，更新下面的进度记录**，便于换窗口后无缝接续。

---

## 八·补、四套主题人格（后续所有端都按这张表做，不是换色，是由内到外换设计）

★ **命名校准（2026-09-20，别再叫错）**：UI 显示名 = **经典版 / 松墨绿 / 深色 / 青绿**，**出厂默认 = 经典版**。
`warm` 只是**内部 key**（历史名叫「暖橙」，2026-09-19 配色已整体换成松墨绿 `#177A56→#0F5E45`）。
**暖橙这套配色已不存在**，文档与注释一律写「松墨绿」，再写暖橙就是误导（历史记录里的旧称不动）。

| 维度 | 经典版 classic（出厂默认） | 松墨绿 warm | 深色 dark | 青绿 fresh |
|---|---|---|---|---|
| 气质 | 原始基线 | 稳重厚实 | 紧凑终端 | 通透轻快 |
| 顶栏/侧栏 | 深蓝渐变（不动） | 白顶栏 + 深墨蓝侧栏 | 顶栏侧栏一体 #131A22 | 白顶栏 + 纯白侧栏 |
| chrome 高度 | 60px（不动） | 64px | 56px | 68px |
| 导航选中 | 基线 | 10px 圆角 + 左侧主色竖线 | 方正 4px + 主色字 + 左侧竖线 | 胶囊 999px + 淡青底 |
| 卡片 | 4px 顶部色条 | 纯白卡 + 柔和阴影、无框 | 卡面色 + 内高光 | 极淡 1px 边框 + 20/24px 大圆角 |
| 表格 | 基线 | 宽松 52px + 1px 极淡横线 | 行条 2px 间隙、无横线、表头大写 | 独立圆角条 6px 间隙 56px |
| 按钮圆角 | 9px | 10px 实心主色 | 6px 透明底 + 主色描边 | 999px 胶囊实心 |
| 间距节奏 | gap18/行44 | gap20/行52/留白28 | gap14/行44/留白20 | gap28/行56/留白36 |
| 手感 | — | .18s、hover 上浮 1px + 橙光晕 | .12s、只变色不位移 | .22s 弹性、上浮 2px |

推进顺序：开发者端（chrome → 组件形态 → 密度手感）→ 商家端 → 供应商端 → uni-app。

**分批计划（一次只做一批，做完给用户看，确认后再进下一批）**

| 批次 | 范围 | 涉及文件 | 回退开关 |
|---|---|---|---|
| 第 0 批 | **清账**：把第九节「待用户验证」的 30+ 项逐端逐主题验收，问题记回本文档 | 不改代码 | — |
| 第 1 批 | 表格形态差异（warm 宽松横线 / dark 行条 2px 间隙无横线 / fresh 独立圆角条 6px 间隙） | `theme.css` 密度段 | 注释掉该段 |
| 第 2 批 | 卡片边界语言（阴影 / 边框 / 色块 / 留白）在商家端 + 供应商端落地 | `theme.css` + 两端内联 | 摘 link |
| 第 3 批 | 按钮圆角 + 手感在三端统一（9 / 10 / 6 / 999px，.18s 上浮 / .12s 只变色 / .22s 弹性） | `theme.css` | 摘 link |
| 第 4 批 | 供应商端人格化（该端刚接通主题，形态尚未拉开） | `theme.css` + `supplier-dashboard.html` | 摘 link |
| 第 5 批 | uni-app 跟进 | `uni-app/` | 单端 |

每一批都要：**改前快照 → 只改本批范围 → 回报「改了哪几行、改前改后」→ 等确认**。

---

## 八·再补、供应商端沉思（2026-09-18 确立，后续所有端遵照）

### 最高纲领：**好用第一位，好看第二位**
小程序是给客户干活用的工具，不是作品集。**先保证少点几下、少动一次脑子、少出错，
再在这个基础上变好看。** 任何"为了好看"却增加点击、隐藏信息、缩小字号的改动，一律作废。
产品定位：免费提供给供应商，但按**高价值付费产品**的水准去做（William 原话：我们的定位就是高档实用的产品）。

### 供应商端三大高频动作（按工作强度排序，改方案先看这张表）
| 排序 | 动作 | 现实 | 设计目标 |
|---|---|---|---|
| 1 | **今日改价** | 天天做，二级市场的蔬菜**必须天天改**，商品多、手输易错、错价即亏钱 | 把重复苦役压到最短：沿用昨日、键盘流水线、批量调整、涨幅警示、低于成本拦截 |
| 2 | 上架 / 编辑商品 | 一次性工作量大、很累 | 批量、模板、复制相似品，减少重复录入 |
| 3 | 待处理订单 / 分拣过秤 / 配送单 | 天天看、天天操作 | 一屏看清、少滚动、状态色一眼分辨 |

### 已确认的功能缺口：**客服栏目（供应商端缺失）**
供应商供货给中小餐饮，必然产生售后：品质争议、少件漏件、退换货、配送迟到、账款/补差争议。
目前**没有一个闭环**。要做「客服工单中心」：商户发起 → 供应商收到 → 协商/同意退换/驳回/转平台介入 → 全程留痕 → 超时自动升级平台。
价值：纠纷双方有据可依；平台拿到介入权与裁决权；投诉率沉淀成品控数据（哪个供应商投诉率高、哪类问题多）。

### 四套主题人格化的**边界**（回应「每个人种必须内外都不同」）
四套是四个不同的人种，必须由内到外不同；但有一条**不可退让的底线**——人格化不能突破好用。

**可以不同（人格）**：圆角语言（方 / 小圆 / 大圆 / 胶囊）、疏密节奏、边界语言（阴影 / 边框 / 色块 / 留白）、
导航选中方式（竖线 / 胶囊 / 底色 / 色字）、动效手感（时长、是否位移）。

**不许不同（好用底线，四套必须全部达标）**：
① 正文对比度 ≥ 4.5:1；② 点击热区 ≥ 44px（手机全程手操作）；③ 数字字号不得低于可读性下限（已由 `FIT_MIN` 守住）；
④ 红黄绿状态色在四套主题下都必须一眼分清；⑤ 关键动作不许靠 hover 才出现。

**四套主题对应的是真实工作场景，不只是审美偏好**：
经典版 = 老用户的肌肉记忆，不变就是效率；松墨绿 = 白天档口长时间盯屏，绿色最放松；
**深色 = 凌晨备货 / 夜间分拣的人（供应商常在凌晨 2-4 点干活，暗光下白屏刺眼，深色是护眼工具不是炫技）**；
青绿 = 生鲜品控岗与年轻管理者，通透、强调新鲜度。

---

## 九、进度记录（每完成一类请更新）

| 类别 | 状态 | 改了什么 | 验证结果 |
|---|---|---|---|
| 第 1 类 · fresh 白底白字 | 已改（待验证） | `admin.html:64` 品牌名、`101-118` 退出登录、`132/139` nav-tool 全部改读 `--sidebar-*` 令牌；`theme.css` 四套主题各补 4 个令牌 | 待用户验证 |
| 第 1 类 · 退出登录置底 | 已改（待验证） | 新增 `.sidebar-foot`（sticky bottom:0 + 不透明打底），按钮外套一层 `div`，`admin.html:101-118` + `:2584` | 待用户验证 |
| 第 1 类 · 内容区居中 | 已改（待验证） | `admin.html:151-154` 新增 `.main > * { max-width:1100px; margin-left:auto; margin-right:auto }` | 待用户验证 |
| 第 2 类 · 供应商端接主题 | 已改（待验证） | ① head 引入 `/theme.css`（放在内联 `<style>` **之前**，让本页样式优先级更高、外观主导权仍在本页）；② 删除内联 `:root` 里 11 个写死老令牌（`--nav/--nav-2/--accent/--accent-2/--green/--gray/--red/--bg/--line/--radius*/--shadow*`），全部改由 theme.css 提供（四套主题各一套）；③ head 加预加载，渲染前写入 `data-theme`（默认 warm，与商家端共用钥匙 `dhAdminTheme`），避免先按默认色画一遍再跳变；④ `<body>` 加 `class="supplier"` —— theme.css 里 3 条 `.supplier` 演示横幅避让规则一直挂着没生效，缺的就是这个 class；⑤ 补主题入口：右上角 `.theme-fab` 按钮 + `#themeModal` 弹窗 + 切换 JS，样式全部复用 theme.css 现成的 `.theme-fab/.theme-panel/.theme-card`（1130 行那段注释写明是 admin.html / supplier-dashboard.html 共用，故无需另写） | 待用户验证 |
| 第 3 类 · fresh 总览少一个框 | 已改（待验证） | 根因：商家端青绿统计卡规则（原 `theme.css:1297-1333`）没加 `.admin` 作用域，泄漏到开发者端，把「订单总额卡」与供应商卡压到同一格重叠。已给该组选择器全部补 `.admin` 前缀，商家端布局不变；开发者端青绿恢复为「左大（订单总额）+ 右两小」三框 | 待用户验证 |
| 商家端主题版上线（重要） | 已改（待验证） | **发现：之前窗口做的商家端主题改造一直写在 `admin.html.bak` 里，线上 `admin.html`（4320 行、硬编码颜色）根本没接主题**。已把主题版（4581 行，含 `/theme.css` 引用 + `dhAdminTheme` 预加载 + 主题切换器）合并回 `admin.html`，旧版备份为 `public/admin.html.20260918-pretheme.bak` | 待用户验证（先看商家端功能是否正常） |
| 第 3 类 · 商家端 chrome 人格 | 已改（待验证） | `theme.css` 末尾新增 `.admin` 段：暖橙侧栏改深墨蓝 `#242C3A→#1A212C`（不再与经典版同色）、10px 圆角 + 选中态左侧主色竖线；深色侧栏 `#131A22` 一体、导航方正 4px + 主色字；青绿白侧栏 + 胶囊选中；**修掉青绿白侧栏下品牌名/退出按钮/.nav-tool 悬停写死 `#fff` 导致隐形的问题**。商家端侧栏固定 240px（`.main` 写死 margin/width），故不动宽度 | 待用户验证 |
| 商家端 bug · 统计卡图标与数字重叠 | 已修（待验证） | `.home-stat .stat-ico-line` 原为 `position:absolute; top/right:22px`，而暖橙/深色/青绿把数字设成 32/30/26px 且 `text-align:right`，大号数字被推到右上角正好压住图标（经典版 28px 左对齐所以不暴露）。改为图标走正常流（static，22px）+ 三套主题数字改左对齐；warm 那条 `.stat-ico-line` 绝对定位特异性更高，另用 `.admin` 作用域再压一次 |
| 商家端 bug · 演示横幅遮挡画面 | 已修（待验证） | `demo-banner.js` 是 `fixed + z-index:99999`，只往 body 插 36px 垫片，管不到 `position:fixed` 的侧栏。脚本改为同时给 `<html>` 打 `data-demo-banner`，`theme.css` 新增 `--demo-h` 让 `.sidebar / .main / .topbar / .theme-fab` 统一下移 |
| 商家端 bug · 右上角没有切换按钮 | 已修（待验证） | `.theme-fab` 原先定义在**右下角**（`bottom:24px`）。改到右上角 `top: calc(var(--demo-h) + 18px); right:24px; z-index:200`；移动端仍回右下以避开采购商城结算条 |
| 商家端 bug · 主题弹窗很难看 | 已修（待验证） | 商家端「店名字体 / 菜单排版」选项卡也用了 `.theme-card`（2px 描边 + `.tc-banner` 54px 色条 + `.tc-check` 右上角勾），与主题卡片同名互相污染。主题卡片规则全部收进 `.theme-grid > .theme-card` 作用域，并显式抵消排版卡样式 |
| 清理垃圾文件 | 已做 | 删除 `admin.html.bak`、`admin - 副本.html`、`admin.html.20260918-pretheme.bak`、`theme - 副本.css`、`theme-fix.css`、`theme-min.css`（均无任何页面引用）。`admin.html` 内容等于 git HEAD，可随时 `git checkout -- public/admin.html` 还原；保留 `supplier-dashboard.html.bak` 供供应商端接入时对照 |
| 商家端骨架缺失（没有顶部横幅） | 已修（待验证） | 商家端桌面原本**没有顶栏**：侧栏 `fixed top:0`、主区 `height:100vh` 直接顶到屏幕最上沿，主题按钮是右下角悬浮球——和开发者端「顶栏 + 侧栏」的骨架完全不是一回事，这才是「怎么看都像那个改乱的版本」的根源。现已补常驻 `.topbar`（左：汉堡 + 品牌；右：🎨 + 退出登录），四套主题 `--chrome-h` = warm 64 / dark 56 / fresh 68，侧栏、主区、移动端抽屉、演示横幅全部按它避让；侧栏顶部重复的品牌区已隐藏 |
| 侧栏被横幅压住、工作台点不到 | 已修（待验证） | 侧栏 / 主区的 `top`、`margin-top` 只写了 `calc(var(--chrome-h))`，变量一旦没解析出来整条声明就失效、退回 `top:0`，侧栏整条跑到横幅底下。现改为「静态兜底值 + calc」双写，变量失效也至少让出 56px |
| 汉堡点了没反应（变黑变白） | 已修（待验证） | `.topbar .hamburger` 写了两条，后一条 `display:flex` 把前面的 `display:none` 覆盖了，桌面也显示三条杠；但桌面侧栏常驻、没有抽屉动画，点了只是在侧栏上加个 `.open` 类。现桌面隐藏汉堡，只在窄屏出现并真正开合栏目 |
| 🎨 点了只出一句说明 | 已修（待验证） | 根因是**两个 `id="themeGrid"`**（装修栏一个、主题弹窗一个），`getElementById` 只拿到装修那个，4 张主题卡被渲染进「店铺装修」，弹窗自然是空的。弹窗容器改名 `#themeModalGrid`；同时 `.theme-card` 与装修卡同名互相串样式，两边都加了作用域（`#pane-decorate` / `.theme-panel,.theme-section`） |
| 顶栏右侧按钮语义 | 已改（待验证） | 改成「小后退 / 退出登录」双态：不在工作台时显示「返回」，按访问轨迹一步一步往回退；退到工作台自动变成「退出登录」，再点才真的退出；「退出登录」仍保留在左侧栏目页底部 |
| 演示横幅下侧栏仍被压住 | 已修（待验证） | 根因不在 `admin.html`，而在 `theme.css` 的旧规则 `html[data-demo-banner] .admin .sidebar { top: var(--demo-h) }`——只让了横幅 36px、**没加顶栏高度**，且特异性高于内联 `.sidebar`，把修复覆盖了。现在商家端侧栏 / 主区统一按 `横幅 + 顶栏` 避让 |
| 青绿主题"点进来是灰的" | 已修（待验证） | 主题切换本身正常（`data-theme` 写入无误），是配色问题：fresh 的 `--bg:#F4F7F7` 跟经典版的灰底几乎没差别，只有主色是青的。底色 / 描边 / 浅表面色统一改为透青（`#E8F3F0` 系列） |
| 工作台首卡是"今日采购额" | 已改（待验证） | 首卡是最大最醒目的位置，应该放「赚了多少」而不是「花了多少」。现在主打位放**扫码点餐今日营业额**；没用过扫码点餐时自动换成**鼎恒币余额**（同样是"得到"不是"花掉"）。采购额降级到末位卡并标注「成本参考」，永不占主打位 |
| 统计卡小图标 | 已移除（待验证） |
| **商家端三条铁律（教训）** | 确立 | ① 经典版一个像素不许动 → 任何规则必须挂 `[data-theme]:not([data-theme="classic"])`；② **禁止斑马纹**（一行浅一行深反人类、看着头晕），横向阅读改用「悬停定位当前行 + 首列主色细条」；③ 设计是给人类高效工作用的，不是给 AI 摆比例用的 → **必须先保证一目了然、少滚动**，美观只能在不占空间的手段上加（等宽数字、负字距、双层投影、焦点环、滚动条），禁止用 aspect-ratio / min-height / 放大 padding 去撑「高级感」 |
| 上一版作废原因 | 已重写 | 误伤经典版 + 误加斑马纹 + 把统计卡强制撑成黄金巨框（导致一屏看不全、手机要狂拉） |
| **手机端重排（给人类设计）** | 已做（待验证） | 根因：admin.html `@media (max-width:900px)` 里把 `.home-grid` 写死成 4 列 → 375px 宽每格 74px，「采购商城」必然被劈成两行。改为 2 列 + 图标左/文字右同排不折行。数据卡改一列竖排，每张内部「左标签 / 右数值」横向读，数值 `nowrap`（修青绿 40px 折行与深色挤压）。以上全部带 classic 豁免 |
| 工作台·采购转化入口 | 已做（待验证） | 横幅那句话本身可点（`data-go="mall"`，走 `switchTab`），**不做凸起按钮**——用户明确否决了按钮方案：「原本那句话那个框子能点不就行了」。视觉保持一句话原样，只加光标+悬停底色+按下回弹。文案改回短句（长度≈原句）：「今天还没下单？商城价比市场批发价更低」 |
| **最高原则（反复返工的根因）** | 确立 | 我几次办砸的根因是**超额设计**：你要「一句话能点」我塞颗按钮、你要「修好折行」我把宫格改成横条、你要「高端」我把卡片撑成巨框。正确做法是——**在原有形态上最小修复**，不替用户加戏。形态（宫格就是宫格、一句话就是一句话）属于用户已经认可的东西，不得擅自重设计 |
| 快捷入口（四宫格） | 返工（待验证） | 保持宫格：图标在上、文字在下。**唯一改动是 4 列→2 列**（375px 分 4 份每格 74px 才导致「采购商城」折行）。此前误改成「图标左/文字右」横条，被否决已还原 |
| 深色手机竖排 | 加固（待验证） | 对 `.home-stats` 与 `.stat-grid` 两个容器都强制 `grid-template-columns: 1fr`，并加 `display: grid` 兜底（防止某主题用 flex 导致 columns 失效）。电脑端仍是左右并排 |
| 青绿首卡特征 | 保留（待验证） | 手机上主打那张「今日营业额」仍比其它三张大：padding 20px / min-height 92px / 数字 32px + 主色（桌面为 40px） |
| 高端视觉层（黄金比例） | 新增 `public/theme-pro.css`（待验证） | **必须先摸清加载顺序**：`theme.css` 的 link 在 admin.html 内联 `<style>` 之前，所以内联样式优先级更高、改不动外观。解决办法是把新的视觉层 link 放在内联 `</style>` 之后再用 `.admin` 提权。由此建立：字阶 √φ≈1.272 / 间距斐波那契 4·6·10·16·26·42·68 / 行高 1.618 / 卡片黄金矩形 φ 与 φ² / 双层投影 / 宽屏 1512 黄金阅读尺度 |
| 商家端超越开发者端 | 已做（待验证） | 开发者端仍是单层灰投影 + 14px 平铺排版；商家端多了：双层投影纵深、负字距标题、`tabular-nums` 数字等宽、斐波那契留白、宽屏 p-  financi 1512 居中、滚动条精修、`.admin`  焦点环 | 工作台 4 张「今日数据」卡的 `.stat-ico-line` 线性图标全部删除，只留「数字 + 标签 + 备注」 |
| 主题卡片挤成竖条（四个麻将） | 已修（待验证） | `admin.html` 里「店名字体 / 菜单排版」选项卡也有一个 `.theme-grid`（`minmax(150px,1fr)`），与主题弹窗的 `.theme-grid`（`minmax(238px,1fr)`）**同名冲突**，把 4 张卡压成窄竖条、挤在左侧站不满右边。改为 `.theme-panel .theme-grid / #homeThemeGrid` 提权 + 一行两张占满的横向长方形 |
| 第 3 类 · 开发者端 chrome 形态差异 | 已改（待验证） | `theme.css` 末尾新增 `[data-page="dev"]` 段：新增 `--chrome-h`（warm64/dark56/fresh68）并让顶栏高度与侧栏 sticky/抽屉基准统一，**顺带修掉非经典版侧栏被压掉 4px 的老错位**；暖橙侧栏改深墨蓝渐变 `#242C3A→#1A212C`（不再与经典版同色）、236px、10px 圆角 + 选中态左侧主色竖线；深色顶栏侧栏一体 `#131A22`、212px、方正 4px 导航 + 只变色不位移；青绿 224px、胶囊选中态；三套各自的过渡手感（.18s 上浮 / .12s 无色移 / .22s 弹性） | 待用户验证 |
| 第 3 类 · warm 总览统计卡改进 | 已改（待验证） | `dev-console.html` `<html>` 加 `data-page="dev"` 标记；`theme.css` 末尾新增 `[data-page="dev"][data-theme="warm"]` 段：总览统计卡去掉左侧 3px 竖线，改纯白卡+柔和阴影，标签深色 13.5px 加粗在上，主色 36px 数字左对齐在下，线性图标右上角（对齐 design/warm.png）。只影响开发者端 warm，classic / 商家端不经过 | 待用户验证 |
| 移动端可用性修复（用户手机实测反馈） | 已改（待验证） | ① **数字可读**（最要紧）：`.home-stat b` / `.stat-card .value` 原为 `word-break:break-all`，把 `¥1,234,567.89` 从中间切成 2-3 段，人读不懂 —— 改 `word-break:keep-all` + `overflow-wrap:normal`，另加 `fmtBigMoney()` 满万/满亿折算，单位 `.unit` 独占一行；② **手机单列**：新增 `≤640px` 断点，四张统计卡由 2×2 改上下单列（原来是竖长条，长数字竖着排＝反人类）；③ **入口不再压住退出登录**：`.theme-fab` 是右上角悬浮 `top:18px`，而供应商端顶栏高 60px，必然重叠 —— 改为放进顶栏内走普通文档流（顶栏是深色渐变，故半透明白底＋白图标），手机端回右下角 `bottom:20px`（theme.css 的 78px 是给采购商城结算条留的，本页没有那条）；④ **图标重画**：原 20px 空心小圆在手机上糊成一团，换 lucide 调色盘（实心色点＋1.8 描边）；⑤ **弹窗改底部抽屉**：手机上居中弹窗被四张竖排卡撑很高，关闭 ✕ 被顶出视口，要关还得往上滑去找 —— 改为从底部升起＋标题栏 sticky 吸顶常驻，卡片改两列（改在 theme.css，两端共用） | 待用户验证 |
| 用户复验后的修正（推翻上一批的部分做法） | 已改（待验证） | ① **取消「万/亿」折算**：金额必须精确到分（两位小数＋千分位），几千块不能写成 0.3 万 —— 已回退；② **统计卡改按主题区分**：经典/暖橙手机上是 **2×2 四宫格**（保持），只有**深色**上下罗列（一框一行）、**青绿**也上下罗列但**第一个框放大**作主指标 —— 上一批做成「所有主题一律单列」属擅自扩大范围，已改回；③ **调色盘挪回顶栏**：放在「返回/退出登录」**右边**，与商家端、开发者端一致；手机上也不放右下角（顶栏挤就把「您好，XXX」收起来）；④ **图标改彩色**：四个色点用彩色，不再用 currentColor 单色（此前被吐槽「黑白、像没画完」）；⑤ **右上角按钮改「小返回」**：维护 `_tabStack` 层级栈，非初始页回上一层，停在初始页（工作台）才等于退出登录；⑥ **侧栏最底部加「一键退出登录」**（红底），并把引导页 gate 的退出从 `$('logoutBtn').click()` 改为直接调 `doLogout()` —— 否则该按钮变成「返回」后引导页就退不出去了；⑦ **修复侧栏「工作台」看不见**：`theme.css` 横幅避让只让了横幅没让顶栏（`top: var(--demo-h)`），侧栏被推进 sticky 顶栏背后，第一项「工作台」被盖住，露出的第一项成了「待处理订单」—— 改为 `calc(var(--demo-h) + 60px)` | 待用户验证 |
| 第七轮修正（按钮变白的终极根因 / 数字实测自适应） | 已改（待验证） | ①**按钮在三套主题下「变浅、直接是白色」的终极根因**：theme.css **722 行** `[data-theme]:not([data-theme="classic"]) .btn { background: var(--surface-3); }` 把暖橙 / 深色 / 青绿三套主题里**所有 `.btn`** 的背景刷成极浅灰，特异性 **(0,3,0)**，压过本页 `.btn`(0,1,0) 与 `.home-todo-item .btn`(0,2,0) —— 浅灰底 + 白字 = 整颗按钮隐形；**经典版恰好不在 `:not()` 范围内，所以只有它没事**，与用户反馈逐字吻合。前几轮调 `--primary`/`--accent` 全是无用功，问题出在层叠。修：加 `html[data-theme]` 前缀把特异性抬到 (0,3,1) 抢回主色实心；描边类按钮的 `border` 也要同样抢回（theme.css 870/1447 给了 `border:none`）；dark `#FF9463` / fresh `#00B8A9` 主色偏亮，实心按钮改深字保证对比度。②**统计数字**：theme.css 1354(warm 32px) / 1388(dark 30px) 是**写死值**，手机上 2 列窄框放不下长金额 → 被 `.home-stat` 的 `overflow:hidden` 裁掉一截；而 `clamp(vw)` 只能按视口猜，同排「0」和「¥1,234,567.89」长度差十几倍，一个猜测值必然顾此失彼。改成用户要的规矩：**CSS 给足基准（桌面 32px / 手机 26px，且用 `html[data-theme]` 压过写死值）+ JS 实测 `fitStatNumbers()` 逐 1px 下调到刚好放得下**（下限 15px，不会掉成蚂蚁）。③ 退出登录：与上方拉开一行间距（`margin-top:22px`）、左内边距收到 10px 更靠左 | 待用户验证 |
| 第六轮修正（待办按钮 / 退出降噪 / 双栏独立滚动 / 数字字号） | 已改（待验证） | ①**待办「去改价 / 去处理」依旧淡**：`.home-todo-item .btn` 写死 `color:#fff`，而 dark 的 `--primary` 是 `#FF9463` 浅橙、fresh 是 `#00B8A9` 亮青 —— 白字压亮底＝看不清（与 btn-green/btn-red 同一个坑）。改按主题给深色字（dark `#6b2d0c` / fresh `#003d38`），用 `:not()` 排除描边类按钮免得改坏。②**退出登录太突兀**：实心大红块 + 图标放大，比其他导航项大一圈 → 改回与「数据统计 / 进件中心」完全同尺寸同排版，透明底 + `#f87171` 红字 + hover 淡红底，下移一行间距。③**左栏边框只画一半 / 底部小字溢出**：`min-height` 撑不满 → sidebar 与 main 都改 `height: calc(100vh - 60px)` + `overflow-y:auto`，**左右两栏各自独立滚动、都覆盖到底**（商家端本就是这个结构；**开发者端是同样毛病，一并统一**）；手机端整页一起滚。④**数字太小**：`.home-stat b` 桌面 `clamp(16px…22px)` → `clamp(22px, 2.6vw, 30px)`，手机 `clamp(13px…)` → `clamp(20px, 5.2vw, 28px)`，dark 同步 —— 先给足字号，只在数字真的变长时才逐步缩小到不溢出，不再一上来就往小里给 | 待用户验证 |
| 第五轮修正（按钮对比度 + 文案 + 退出按钮位置） | 已改（待验证） | **按钮在三套主题下淡到看不见的根因**：① theme.css 330/331 在深色主题把 `--c-green` 换成 `#34D399`、`--c-red-500` 换成 `#FF7A7A`（都是浅色），而 `.btn-green/.btn-red` 写死 `color:#fff` —— 白字压浅底＝隐形；② 暖橙 `--border:#EBEDF0`、青绿 `#D6E6E1` 极浅，`.btn-ghost` 白底配这种边框＝边界消失；③ 待办按钮只写 `class="btn"`，而 `.btn` 基类**压根没写 background/color**，吃浏览器默认灰底黑字。修复：`.btn` 基类补品牌主色实心；编辑/下架改白底＋中灰描边＋深字；上架白底深绿字、删除白底深红字（不再依赖会被主题换浅的变量）。**退出登录**：`#fca5a5` 淡红字＋12% 底 → 改实心红底白字；位置从底部小字**下方**挪到「数据统计」正下方（小字**上方**），不再贴边溢出。**文案**：「不变」→「沿用」（沿用比不变有档次），按钮、本类一键确认、toast、confirm、保鲜期横幅、注释全部同步 | 待用户验证 |
| 第四轮修正（深色竖排 · 真根因） | 已改（待验证） | **前几轮改不动的真正原因**：theme.css 1366 在深色主题下把 `.home-stats` 改成了 **`display:flex` 横排**（桌面端四张并成一整块仪表盘）。我一直改的是 `grid-template-columns` —— 对 flex 容器这是**无效属性**，所以界面纹丝不动。修复：手机端（≤900px）改写 `flex-direction: column`（一框一行），分隔线由左边框改下边框、最后一张不画线；电脑端保持 theme.css 的 flex 横排不变，仍左右罗列。同时用 `[data-theme="dark"][data-theme] .home-stat b` 压过 theme.css 1388 **写死的 30px 字号**，手机上改 `clamp(15px,4.2vw,22px)`，避免长数字溢出 | 待用户验证 |
| 第三轮修正（照搬商家端，不再自造轮子） | 已改（待验证） | ① **调色盘照搬商家端 admin.html:2645**：改用 theme.css 的 `.theme-icon-btn` + 🎨 emoji，删掉本页自己写的 `#themeFab{color:#fff}`。根因：本页 `<style>` 加载在 theme.css **之后**、优先级更高，自己写死白色会压掉 theme.css 742 的 `[data-theme="warm"] .topbar .theme-icon-btn` 浅色适配 —— 暖橙/青绿顶栏转浅色后就是**白底白图标，按钮直接看不见**；② **深色单列漏了一半**：只写了首页的 `.home-stats`，漏了数据统计页的 `.stat-grid`，已补；同时改用 `[data-theme="x"][data-theme]` 提高优先级（与 theme.css 754 同一手法），否则压不过 theme.css 主题视觉层、手机上会退回 2×2；③ **金额超框**：整数段/小数段拆成 `.int` / `.dec` + `text-align:right`，放不下时小数整体掉到第二行右侧；字号 `clamp(13px,3.2vw,22px)` 自适应缩小，**绝不溢出卡片边框** | 待用户验证 |
| 第 3 类 · 四套主题结构差异 | **部分完成（2026-09-20 盘点）** | 已完成：令牌层（`--btn-radius` 9/10/6/999、`--radius-card` 16/16/10/20、`--gap`、`--row-h` 52/44/56、`--section-gap`、`--page-pad`、`--card-shadow`、`--field-*`，四套各一组）+ 开发者端 chrome 形态 + 商家端 chrome 形态 + 开发者端 warm 统计卡 + **表格形态三端落地（2026-09-18，theme.css 1800-1931）** + **卡片边界语言（商家端随「松墨绿质感增强层」与令牌层落地；供应商端 2026-09-20 落地，theme.css「第 2 批」段 1933+）**。**全部完成**（第 1~5 批均已落地，待用户逐批验收）。分批计划见「八·补」末尾 | 待用户验收 |
| 第 5 批 · uni-app 跟进（小程序端收尾） | 已改（待验证） | 盘点结论：小程序端**本来就已接好主题**（`utils/theme.js` 四套切换 + 系统深色跟随 + `applyChrome()` 改写原生导航/tabBar；`styles/theme-vars.css` 四套令牌；`uni.scss` 颜色/圆角/阴影全部改写成 `var(--dh-*, 回退值)`，形态差异由令牌提供，无需另做）。本次修的是**换肤串色**：① `theme.js` 的 `CHROME.warm` 导航色还是旧暖橙 `#FF7A3D`（松墨绿上线后没同步）→ 改 `#177A56`，tabOn 同步；② `CHROME.dark` tabOn `#FF9463` → `#FFA873`、tabBg `#182029` → `#1B2634`，与 `theme-vars.css` 的 `--dh-primary / --dh-card` 取值对齐；③ `CHROME.fresh` tabOn `#00B8A9` → `#00A078`（令牌主色）。并写下硬规矩：**这张表与 theme-vars.css 必须同值，改一边忘了另一边就是串色**。④ `pages.json`「订单管理」页写死 `#1f1f1f` 深黑导航（其它页都是品牌橙），开屏瞬间不是一个牌子 → 统一 `#FF6B35`，背景 `#121212` → `#ffffff`（进页后仍由 applyChrome 按主题改写）。**未做**：uni.scss 的间距阶梯刻意不参与主题（改间距会让每页重排，风险远大于收益，文件头已写明）。回退：还原 `theme.js` 的 CHROME 与 `pages.json` 该页 | 待用户验证 |
| 第 5 批·补 · 小程序端两处真缺口（KDS 换肤 + 深色主题页面底色） | 已改（待验证） | 盘点确认小程序端**形态差异已由令牌提供**（圆角 `--dh-radius-*` / 按钮圆角 `--dh-btn-radius` / 阴影 `--dh-shadow*` 四套各一组，uni.scss 全部改写成 `var(--dh-*, 回退值)`，页面零改动即继承；间距 `$sp-*` 刻意不参与主题，改间距会让每页重排，风险远大于收益）。剩下两个真缺口本次修掉：① **后厨 KDS 换不了肤**（`pages/admin/order.vue` 整页写死中性深灰 `#121212/#1e1e1e/#2a2a2a/#888`，换主题只有主色变，底色永远中性黑）。新增 KDS 专用令牌组 `--dh-kds-{bg,bar,card,chip,line,text,title,sub,done-bg,done-text,accent,glow}`（四套各一值 + 经典版基线 = 改造前字面值），页面色值全部改 `var(--dh-kds-*, 原值)`，**经典版零变化**。设计决定：KDS **四套一律深底**（后厨强光＋连续盯屏几小时，浅底眩光费眼，深底是功能需要不是配色偏好），换主题变的是**色相**——经典版中性黑 / 松墨绿墨绿底 `#0F1A15` / 深色冷蓝黑 `#0E1620` / 青绿青调黑 `#0B1A16`；强调色一律用 `--dh-kds-accent`（主色在近黑底上的**提亮版**：`#FF9A73`/`#A8D9C4`/`#FFD8B4`/`#6FE3CB`）—— 直接用主色太暗，松墨绿 `#177A56` 压在近黑底上几乎分不出，出餐位会看错数量；新单闪烁光同步走 `--dh-kds-glow`（万一某些 WebView 不解析 keyframes 里的 `var()`，最坏只是不发光，仍有左侧强调色条，**不会漏单**，故不做降级兜底）；② **深色主题下页面底色仍是浅灰**（真 bug）：`App.vue` 的 `page` 写死 `#f5f5f5`，而主题 class 挂在**页面根 view**（page 的子节点），CSS 变量只往下继承 —— page 自己永远只拿得到经典版基线值，结果「深色卡片浮在浅灰页面上」。修法：`page` 底色走 `var(--dh-bg, #f5f5f5)`（经典版 `--dh-bg` 同步改成 `#F5F5F5`，与改造前一致、零变化），并让挂了主题 class 的根 view 自己铺满整屏底色（`.theme-warm/.theme-dark/.theme-fresh { min-height:100vh; background:var(--dh-bg) }`）；系统深色媒体查询的写死值改成与 `.theme-dark` 同一组（`#0E1620`/`#C6CDD6`），两边不许各写各的。**明确不做**：hover 手感（小程序没有指针悬停，`:active` 在 view 上不可靠，需改 `hover-class`，12 个页面逐个改、收益低）；间距不参与主题（同上）。回退：还原 `theme-vars.css` 的 `--dh-kds-*` 与 `order.vue` 的 style 段 / `App.vue` 的 `page` 与根 view 规则 | 待用户验证 |
| 第 4 批 · 供应商端整体人格化（一次做全） | 已改（待验证） | 供应商端此前只落了表格/卡片/按钮三批，chrome、导航、容器底色、间距、手感五个维度仍是经典版的形（换主题只换色＝换皮不换人）。本次在 `theme.css` 末尾新增「第 4 批」段，五维一次拉开：**chrome 高度**引入 `--sup-chrome`（松墨绿 64 / 深色 56 / 青绿 68），顶栏、侧栏 sticky、主区高度三处同步，**classic 不定义该变量、回退 60px 与改造前完全一致**；**松墨绿**＝白顶栏 + 深墨蓝侧栏 `#242C3A→#1A212C` + 导航 10px 圆角 & 左侧主色竖线；**深色**＝顶栏侧栏一体 `#131A22` + 导航方正 4px & 主色字 & 左侧竖线；**青绿**＝白顶栏 + 纯白侧栏 + 导航胶囊 & 淡青底（顺带修掉白侧栏下 `#cbd5e1/#94a3b8` 灰白字淡到看不见的问题）。**容器**：本页内联写死 `#fff` 的十几处容器（.card/.settings-card/.placeholder/.empty-tip/.modal-box/.alert-box/.gate-card/.home-stat/.stat-card/.order-card/.qual-status-card）统一走 `var(--card)`，深色加 `1px` 描边 + 顶沿内高光；`.table-wrap` 只在桌面端刷底（手机端本页已改 transparent，跟着刷会顶出多余板）。**深色文字**：容器换底后写死的 `#1f2937/#111827/#475569` 会隐形，统一回 `var(--text)`（状态 chip 各带类名，不动）。**间距**：`--sup-gap/--sup-pad` 20·14·28。**手感**：卡片悬浮松墨绿上浮 1px / 青绿 2px / 深色只变色不位移。另把 demo-banner 避让里写死的 60px 改成 `var(--sup-chrome, 60px)`。冒烟：真 CSS 括号全配平（注释里的中文半角括号不影响解析）。回退：注释掉 `theme.css` 末尾「第 4 批」段 | 待用户验证 |
| 第 4 批·补 · 供应商端整体人格化收尾（换底 + 统计卡形态 + 间距） | 已改（待验证） | 骨架那批做完后，仍有一批零件裸奔在人格体系外，本次一次补齐（theme.css 末尾「第 4 批」段续写）：① **换底**：内联写死 `#fff` 的 11 处容器（`.report-hero` 报表头 / `.tier-progress-card` 档位卡 / `.ob-card` 新手任务卡 / `.qual-tile` 资质上传格 / `.agreement-text` 协议正文 / `.agm-modal`+`.agm-body` 协议弹窗 / `.sort-order` 分拣单 / `.deliver-card` 配送卡 / `.home-todo-item` 首页待办 / `.tk-summary` 工单汇总）统一走 `var(--card)` + 主题字色 —— 深色下这些全是一块块白板，最伤整体感；三套各给边界语言（松墨绿阴影无框 / 深色顶部 1px 内高光 / 青绿极淡 `#E6F0EC` 边框 + 大圆角）。**不动**：`.receipt` 小票（模拟打印小票，本来就该是白的）、状态色条（`.tk-card.urgent/.intervened`、`.order-card` 左侧色条）、选中态（`.tk-sum-card.active`）；② **统计卡形态（桌面）**：松墨绿＝纯白卡 + 柔和阴影无框 + 间距 20；深色＝四张**并成一整块仪表盘**（flex + 1px 分隔线，悬浮只变色不位移）；青绿＝**首卡放大作主指标**（3 列网格占 2/3 宽 + 主色 2px 描边）。手机端不动（沿用内联已调好的 2×2 / 竖排）；③ **收尾**：相邻卡片（`.card + .card`）间距走 `--sup-gap`、`.card-head` 内边距按主题分档（18 / 14 / 22）、侧栏分组标题（`.nav-group-title`/`.nav-tip`/`.nav-divider`）在两套深色侧栏上提亮一档（与经典版进化层同口径）。全部带 `html[data-theme]` 提权 + `.supplier` 作用域 + 非 classic。回退：注释掉 theme.css「第 4 批」段中 A/B/C 三小节 | 待用户验证 |
| 第 3 批 · 按钮圆角 + 手感（三端统一） | 已改（待验证） | 新增手感令牌（四套各一组）：`--btn-dur` / `--btn-ease` / `--btn-lift` / `--btn-glow` —— warm .18s + 上浮 1px + 松墨绿光晕、dark .12s + 抬升 0（只变色不位移）+ 无光晕、fresh .22s 弹性曲线 + 上浮 2px + 青绿光晕。统一层（theme.css「第 3 批」段）把**三端真按钮**的圆角统一到 `--btn-radius`，并给上过渡/悬浮抬升/按下回落；classic 不经过（由「经典版进化层」的 .18s 上浮+橙光晕负责，避免两套动效打架）。覆盖 .btn 全家 + .btn-add/.btn-cart/.sup-action/.pager button/.tier-add/.tier-del/.btn-qa-* + 供应商端 .tk-btn/.pw-act-btn/.pw-carryall-btn/.gate-btn/.qt-btn/.qual-submit-btn/.agm-agree-btn。**不动**：标签徽章（.tk-badge 999px 是标签不是按钮）、圆形图标钮、输入框。**待老板定夺**：人格表写 warm 按钮 10px，但代码里 warm 的 `--btn-radius` 是 999px 胶囊（令牌层后来改的），两套胶囊会削弱区分度 —— 是改回 10px 还是维持胶囊，等你一句话。回退：注释掉 theme.css「第 3 批」段 | 待用户验证 |
| 第 2 批 · 卡片边界语言（供应商端落地） | 已改（待验证） | 供应商端内联样式把卡片写死 `#fff` + 浅阴影，换到深色/青绿主题后边界语言没跟上（深色下仍是一堆白卡）。落地方案（theme.css「第 2 批」段）：**松墨绿**=纯白卡+双层柔和阴影+顶部玻璃高光；**深色**=卡面色（var(--card)）+顶部 1px 内高光、无外阴影；**青绿**=极淡 1px 青绿边框+20px 大圆角+几乎无阴影。覆盖：.card / .pw-summary / .pw-group / .order-card / .placeholder / .empty-tip / .tk-card / .tk-sum-card（统计卡不碰——仪表盘形态已有专门规则）。规矩：状态色条（order-card 左条 / tk urgent / intervened）与选中态（tk-sum-card.active）是功能不是装饰，青绿的整条 border-color 刷色后逐个恢复。回退：注释掉 theme.css「第 2 批」段即可 | 待用户验证 |
| 第 4 类 · 清理写死色与 emoji | 未开始 | | |

### 备注
- **回退快照已完成**（2026-09-18）：`git commit -m "改版前快照"`（commit `ca8a7aa`），
  另存 `public/admin.html.bak`、`public/supplier-dashboard.html.bak`。
- **新增令牌（theme.css，四套主题各一组）**：`--sidebar-strong-color`（品牌名/退出登录文字）、
  `--sidebar-fill`（按钮底）、`--sidebar-fill-hover`、`--sidebar-sticky-bg`（sticky 打底，必须不透明）。
  classic / warm / dark 取值 = 原写死值，视觉不变；仅 fresh 变（白底 → 深墨字 `#1F2329` + 淡底 `#EEF3F2`）。
- **踩坑**：本窗口对 `public/theme.css` 用编辑工具写入会静默失败（文件 mtime 不变、内容不落盘），
  但用 node 脚本 `fs.writeFileSync` 可以正常写。**后续再改 theme.css 请直接用脚本写。**
- `theme.css` 末尾 IDE 报「应输入 }」是**既有误报**（文件末尾未闭合，浏览器自动补全，HEAD 版本同样如此），
  与本次改动无关，不要去动它。
- 第 1 类未动顶栏、未动 classic 的布局/间距/字号。额外顺带改了 `.nav-tool:hover`（同属白底白字同一处病，
  classic 取值一致，视觉不变）。
