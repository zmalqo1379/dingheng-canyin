# 鼎恒科技系统全量文档

> 本文档基于项目源码逐文件通读整理，只记录事实，不含建议。
> 覆盖范围：Node 后端（server.js + routes/ + models/ + middlewares/ + utils/）、Web 四端前端（public/）、uni-app 小程序工程。
> 状态图例：✅ 完成 ｜ 🟡 简陋/模拟 ｜ ❌ 占位 ｜ ⬜ 未做

---

## 1. 技术架构

### 1.1 技术栈

**后端**

| 依赖 | 版本 | 用途 |
|---|---|---|
| express | ^4.19.2 | Web 框架，入口 server.js |
| mongoose | ^8.5.0 | MongoDB ODM，19 个模型 |
| jsonwebtoken | ^9.0.3 | 三端 JWT 鉴权（7 天过期） |
| bcryptjs | ^2.4.3 | 密码哈希（pre-save 钩子，10 rounds salt） |
| multer | ^2.3.0 | 图片上传（菜品图/资质图，2MB 限 jpg/png） |
| node-cron | ^3.0.3 | 定时任务（每日批处理 + 每月月结） |
| cors | ^2.8.5 | 跨域 |
| dotenv | ^16.4.5 | .env 环境变量 |

**Web 前端**：原生 HTML + CSS + JavaScript（无框架、无构建），由 Express 静态托管 `public/`；图片等上传资源托管于 `/uploads`。

**小程序端**：`uni-app/` 目录为独立工程（uni-app/Vue 单文件组件 + Vite，含独立 package.json），与主 Node 服务并行存在。

### 1.2 目录结构

```
dingheng-canyin/
├── server.js                  # Express 入口（公共 API + 商家 CRUD + 上传 + 启动引导）
├── package.json               # start/dev 脚本均为 node server.js
├── .env                       # MONGODB_URI、PORT（git 未跟踪）
├── .gitignore                 # UTF-8 编码，排除 node_modules/、.env
├── middlewares/
│   ├── auth.js                # JWT 签发/校验、shopId 提取、requireMerchant/Supplier/Dev
│   └── featureCheck.js        # checkFeature(featureName) 按会员等级校验功能权益
├── models/                    # 19 个 Mongoose 模型（见第 3 章）
│   ├── Admin.js / ShopAccount.js / Supplier.js / Member.js
│   ├── Category.js / Dish.js / Table.js / Order.js / Setting.js
│   ├── CoinHistory.js / CoinRule.js / Coupon.js / CustomerPoint.js
│   ├── Marketing.js / PlatformConfig.js
│   ├── PurchaseOrder.js / RebateRule.js / RebateSettlement.js / SupplyProduct.js
├── routes/
│   ├── auth.js                # 三端注册/登录/改密/dev seed
│   ├── coin.js                # 鼎恒币兑换会员/兑换券、币状态、流水、月进度、品类倍率
│   ├── purchaseOrders.js      # 采购订单全流程（下单/确认/发货/收货/用券）
│   ├── dev.js                 # 开发者控制台全部接口（31 个路由）
│   ├── supplier.js            # 供应商档案/资质/协议/通知设置/结算/新手任务
│   ├── supplyProducts.js      # 供应商品 CRUD + 公开商品目录
│   ├── marketing.js           # 营销活动 CRUD + 公开 active 查询
│   └── customerPoints.js      # 顾客积分配置/查询/抵现结算/积分换菜
├── utils/
│   ├── dhConfig.js            # 会员定价、币汇率、抵用券面额表、功能权益矩阵等平台常量
│   ├── dhCron.js              # 定时任务（每日过期批处理、每月返点结算）
│   ├── coinRule.js            # 品类倍率计算、采购得币 calcOrderRewardCoin
│   ├── rebate.js              # 阶梯返点计算、订单预估、月结 settleMonth
│   └── marketingCalc.js       # 下单优惠 computeDiscount（先折扣后满减）
├── public/                    # Web 前端（静态页）
│   ├── index.html             # 品牌门户（三端入口）
│   ├── merchant-login.html    # 商家登录/注册（含欢迎弹窗）
│   ├── supplier-login.html    # 供应商登录/注册（含欢迎弹窗）
│   ├── dev-login.html         # 开发者登录
│   ├── admin.html             # 商家后台（12 栏目单页）
│   ├── js/admin.js            # 商家后台逻辑
│   ├── customer.html          # 顾客点餐页
│   ├── js/customer.js         # 顾客点餐逻辑
│   ├── kitchen.html / kitchen.js   # 后厨看单
│   ├── bigscreen.html / js/bigscreen.js  # 大屏展示
│   ├── supplier-dashboard.html     # 供应商后台（6 栏目 + 状态闸门页，逻辑内联）
│   ├── dev-console.html            # 开发者控制台（8 栏目，逻辑内联）
│   └── privacy.html / terms.html   # 隐私政策 / 服务条款静态页
├── uploads/                   # 上传图片存储（git 未跟踪）
├── uni-app/                   # 小程序工程（独立 package.json + vite.config.js）
│   └── src/
│       ├── pages/admin/{index,menu,order,stats}.vue
│       ├── pages/customer/index.vue
│       ├── utils/{request,cart,audio}.js
│       ├── static/{audio/new-order.wav, tabbar/*.png}
│       └── App.vue / main.js / manifest.json / pages.json / theme.json / uni.scss
├── check_data.js / cleanAdminPassword.js / cleanFakeData.js
├── migrate_password.js / migrate_shopid.js / seedProducts.js   # 一次性运维脚本
└── 系统档案.md                 # 旧版项目文档
```

### 1.3 启动方式

- 安装依赖：`npm install`
- 启动服务：`npm start` 或 `npm run dev`（二者均为 `node server.js`）
- 端口：`process.env.PORT || 3000`
- 启动时自动执行：
  1. 连接 MongoDB（失败则重试并报错退出）；
  2. 确保默认开发者账号存在（用户名 `admin` / 密码 `dingheng2024`，不存在则创建）；
  3. 调用 `startDhCron()` 注册定时任务。
- 修改 server.js / models/*.js 后必须重启 node 进程才生效（无热加载）。

### 1.4 数据库连接方式

- 连接串：`process.env.MONGODB_URI || 'mongodb://127.0.0.1:27017/dingheng_canyin'`
- 生产实际使用 MongoDB Atlas 云库（.env 中 `MONGODB_URI=mongodb+srv://...`），本地无 mongod 进程。
- .env 仅含两个键：`MONGODB_URI`、`PORT`。
- 根目录运维脚本（迁移/种子/查数）必须先 `require('dotenv').config()`，否则会默认连 127.0.0.1:27017 报 ECONNREFUSED。

### 1.5 定时任务清单（utils/dhCron.js，node-cron）

| Cron 表达式 | 时间 | 任务 | 内容 |
|---|---|---|---|
| `0 2 * * *` | 每日 02:00 | clearExpiredCoins | 扫描 CoinHistory 发币批次：到 expireAt（90 天）的批次 remaining 置 0、isExpired=true；Member.dinghengCoin 扣减对应数额（不足扣至 0）；每批次写一条 type=expired 流水 |
| `0 2 * * *` | 每日 02:00 | downgradeExpiredMembers | memberExpire 已到期的会员：memberLevel 降为 basic、memberExpire=null、customerPointsEnabled=false |
| `0 2 * * *` | 每日 02:00 | expireCoupons | status=unused 且 expireDate 已过的抵用券置为 expired |
| `0 3 1 * *` | 每月 1 号 03:00 | settleMonthlyRebates | 对上月所有有采购额的供应商执行月结（utils/rebate.js settleMonth，幂等） |

另导出 `runDailyJob`、`settleMonthlyRebates` 供手动触发；开发者控制台"手动月结"调用同一逻辑。

---

## 2. 四端功能全清单

### 2.0 品牌门户 index.html

| 页面/区块 | 功能描述 | 状态 | 手机端适配 |
|---|---|---|---|
| 品牌门户 | hero 品牌区（主标题"鼎恒科技"38px/800/字距6px，副标题"智能餐饮管理平台"15px 独立成行）；三张入口卡（我是商家→merchant-login.html、我是供应商→supplier-login.html、我是开发者→dev-login.html），统一线性 SVG 图标（stroke 1.8）；footer 隐私政策/服务协议链接 | ✅ | ✅ ≤768px 卡片纵向堆叠，标题 28px/12px 单行不折行 |

### 2.1 顾客点餐端（customer.html + js/customer.js）

> 入口：商家后台"预览点餐页"工具或扫码 URL，必须带 `shopId` URL 参数。移动端优先设计。

| 页面/区块 | 功能描述 | 状态 | 手机端适配 |
|---|---|---|---|
| 店铺头部 shopBanner | 店铺 LOGO（logoImage）、店名（4 种字体栈）、桌号牌 table-pill；预览模式显示 backNav 返回按钮 | ✅ | 移动端优先 |
| 优惠信息条 promoBanner | 紧随店名下方。海报模式：promoPoster 存在时以海报为主（aspect-ratio 15/4、15/8、5/4 对应 small/medium/large 三档，object-fit:cover 圆角 14px），文字轮播 display:none；图片 onerror 自动回退文字轮播。文字轮播：满减/折扣/充值送活动卡 + 积分规则卡（"消费1元=X积分/可换菜、可抵现"），3 秒自动横滑、左右箭头、圆点指示、触摸滑动，尊重 prefers-reduced-motion；无活动且无海报时整条隐藏 | ✅ | 移动端优先，文案手机端优先 |
| 菜单区 | 左侧分类栏 sidebar + 右侧菜品列表；3 种排版（list 列表/large 大图/grid 双列网格）；菜品卡含图片、菜名、描述、月售（SALES 文案）、价格、价格下方"可得 X 积分"小字、加减购步进器；无图时彩色块+emoji 占位图；加载时骨架屏 skeleton | ✅ | 移动端优先；grid 为双列 |
| 购物车栏 cartBar | 底部固定：购物车图标+数量角标、合计金额、凑单提示 cartHint（与优惠条同数据源）、结算按钮；点击展开购物车抽屉 sheet（商品明细、数量增减、清空） | ✅ | 移动端优先；预览模式 cartBar 不隐藏（仅 table-pill 隐藏） |
| 结算页 checkoutPage | 桌台选择/手填桌号；商品明细 ckItems；积分卡 ckPointCard：手机号输入（maxlength=11，选填，localStorage 键 `dh_point_phone_<shopId>` 记忆，填满 11 位自动查询余额）、"本单可得 X 积分"、抵现勾选（100 积分=1 元、单笔最多抵 20%）；优惠明细卡 ckDiscountCard（原价/分类折扣/满减/积分抵现/实付逐项）；备注 ckRemark；提交按钮 ckSubmitBtn | ✅ | 移动端优先 |
| 下单 | POST /api/orders（带 x-shop-id 头、customerPhone、usePoints）；预览模式（preview=1）拦截提交并 toast 提示，不产生真实订单；成功后记住手机号、清空购物车、跳成功页 | ✅ | — |
| 成功页 successPage | 积分播报：恭喜获得 X 积分（spEarnText）、当前余额（spBalanceText）、最低门槛可兑换菜链接（spExchangeLink，点击打开积分换菜弹层）；桌号展示；"继续点餐"；4.5 秒自动关闭 | ✅ | 移动端优先 |
| 积分换菜弹层 pointsMask | 手机号查询余额（ptPhoneInput/ptBalanceRow）、可兑换菜品列表 ptDishList、点击兑换调 /api/points/redeem-dish；兑换成功生成 0 元单进后厨 | ✅（进阶版商家才开放；基础版商家顾客端无积分入口） | 移动端优先 |
| 主题渲染 | body.theme-* 5 套主题 CSS 变量、body.layout-* 3 排版、--font-shopname 4 字体；自定义横幅图加 --banner-layer 遮罩；URL 参数 theme/font/layout/bannerImage/logoImage/shopName 可覆盖（预览用） | ✅ | — |
| toast 提示 | 全局轻提示 | ✅ | — |

### 2.2 商家运营工具页

| 页面 | 功能描述 | 状态 | 手机端适配 |
|---|---|---|---|
| 后厨看单 kitchen.html + kitchen.js | 必须带 shopId（缺失显示遮罩）；每 2 秒轮询 /api/orders?status=pending；首屏将 1 分钟前订单标记为"已播报"不响铃；新订单语音播报（speechSynthesis，文案"您有新的订单，X桌，[首菜]一份"）+ 全屏红框闪烁 + 顶部横幅；语音开关跟随 /api/settings 的 enableVoice（仅拉取一次，用户手动切换后不被覆盖）；首次点击/触摸页面静默解锁语音（浏览器自动播放策略）；状态过滤（待处理/已完成/全部）；完成出餐 PUT /api/orders/:id/complete；顶部统计条（/api/admin/stats）；深色主题样式由 JS 注入 | ✅ | ✅ ≤640px 媒体查询 |
| 大屏展示 bigscreen.html + js/bigscreen.js | 必须带 shopId（缺失遮罩）；深色大字风格、全屏按钮、实时时钟；每 5 秒轮询；新订单 beep 提示音（外链 soundjay mp3）+ 红框 + 语音播报全部菜品；受 enableBigscreen 开关控制（每 30 秒刷新设置）；1 分钟前订单不播报规则同后厨 | ✅（提示音依赖外链 mp3） | ✅ ≤768px 适配 |

### 2.3 商家后台（admin.html + js/admin.js）

> 入口 merchant-login.html（登录/注册 Tab 切换 + 首次访问欢迎弹窗，关闭后 7 天内 localStorage 不再显示，"立即免费使用"跳转注册 Tab，标注"注册即送首月进阶版会员（价值 ¥99）"）。后台为单页 12 栏目侧边栏。

| 栏目 | 功能描述 | 状态 | 手机端适配 |
|---|---|---|---|
| 菜单管理 dishes | 分类与菜品 CRUD（分类：热菜/主食/凉菜/汤品/饮品 5 个默认分类，注册时自动创建）；菜品含名称、价格、分类、图片（/api/admin/upload，前端 compressImage 压缩至 2MB 内）、描述、上下架；新增/编辑弹窗 dishModal | ✅ | ✅ 桌面表格 + 手机卡片 dishesCards |
| 桌台管理 tables | 桌台 CRUD（编号+shopId 复合唯一）、空闲/占用状态；无独立编辑接口（修改走删除+新增） | ✅ | ✅ |
| 订单管理 orders | 门店点餐订单列表、明细、状态查看、完成出餐 | ✅ | ✅ |
| 数据统计 stats | 今日订单数、今日营业额、本月营业额（/api/admin/stats、/api/admin/merchant-stats） | 🟡 仅基础统计（dhConfig 中 reportBasic/reportAdvanced 权益位存在，但无经营报表/顾客画像/损耗分析页面与接口） | ✅ |
| 店铺装修 decorate | 5 主题卡（classic 经典橙/minimal 简约白免费；dark 时尚暗黑/green 清新绿/redgold 国潮红金 进阶版锁定，锁定卡点击 toast 并 goUpgrade）；4 字体（modern/serif/round/hand）；3 排版（list/large/grid）；自定义图片上传：横幅 bannerImage（建议 750×360）、LOGO logoImage（200×200）、优惠海报 promoPoster（750×400），均进阶版权益、可恢复默认；海报三档尺寸 small/medium/large 切换（不限等级）；选中主题浮现"预览/应用"按钮；右侧 sticky 手机框实时预览 updatePhonePreview | ✅ | ✅ |
| 店铺设置 settings | 店名；语音播报/大屏展示/打印机/微信通知开关；打印机 SN/Key；通知手机号；保存走 PUT /api/settings | 🟡 开关与配置齐全；打印机/微信通知实际仅 console 模拟（见第 7 章） | ✅ |
| 会员中心 member | 状态条（等级/到期时间/剩余天数/体验期倒计时 pill）；三张人民币版本卡（基础版 ¥0 默认开通、进阶版 ¥99 热销、尊享版 ¥199 旗舰），人民币购买按钮统一 openPayTip 弹 payTipModal（"线上支付即将上线，联系客服 400-888-6666"）；各卡含"基础版/进阶版全部功能"折叠区 toggleInherit；鼎恒币介绍 4 卡；鼎恒币兑换专区 ez-cards（进阶 3000 币/尊享 5000 币，各带"权益详情"折叠 toggleEzBenefit 默认折叠，底部金色小字"鼎恒币兑换与现金购买权益完全一致，一分不花，同等尊贵"）；底部"会员兑换记录"入口跳币流水 | 🟡 币兑换全链路可用；人民币购买为客服弹窗占位 | ✅ |
| 鼎恒币中心 coin | 币余额、流水列表（/api/coin/history）、兑换会员月卡、兑换抵用券（exchangeConfirmModal 确认）、本月得币与兑换目标进度（/api/coin/month-progress）、券包查看（/api/coin/status 返回未用券与 7 天内到期券） | ✅ | ✅ |
| 采购商城 mall | 等级差异化横幅 mallCoinBanner；本月得币进度卡 mallProgress；空状态引导卡 mallGuide（localStorage 记忆关闭）；两层结构：供应商店铺列表（搜索框+品类标签 storeGrid，仅展示 active+已签协议+已开通接单的供应商）→ 店铺内页（面包屑、左侧分类导航、商品网格、右侧采购车 cart）；采购车含起送价提示（minOrderAmount 默认 300）、抵用券选择、券抵扣、应付金额、预计得币；下单 POST /api/purchase-orders | ✅ | ✅ 手机端悬浮栏 m-float-bar + 采购车抽屉 m-cart-drawer |
| 采购订单 purchase | 状态筛选 Tab（全部/待确认/已确认/已发货/已完成）；订单明细（商品、金额、券、返点预估、得币）；确认收货按钮（POST confirm-receive，收货后发鼎恒币+累计采购积分） | ✅ | ✅ 表格 + 手机卡片 purchaseCardList |
| 顾客积分 points | 基础版显示锁定遮罩 pointsLocked（goUpgrade('advanced')）；返积分比例设置（总开关 pointEnabled + 每元积分数 pointSpendPerPoint，默认 1 元=1 积分）；积分抵现设置（开关 pointDeductEnabled + pointDeductPoints 默认 100 积分=1 元 + pointDeductMaxPercent 默认单笔最多抵 20%）；积分兑换菜品（从自家菜单下拉选菜 + 所需积分输入默认 300，pointExchangeDishes 最多 20 个） | ✅（进阶版权益） | ✅ |
| 营销活动 marketing | 全局锁 marketingLockedAll；满减卡（进阶版权益）；折扣卡（尊享版权益，内联锁 discountLockedInline，必须指定分类）；充值送卡（尊享版权益，卡片注明"储值账户体系后续接入"）；新建/编辑弹窗 mktModal（三类活动字段切换）；新建活动默认停用需手动启用；桌面表格行内操作（编辑/启用开关/删除） | ✅（充值送 🟡：无储值账户，活动仅展示不参与下单计价） | ✅ ≤768px 显示手机卡片 .mkt-card-m，启用开关固定在卡片标题行右端 |
| 店铺运营外链工具 | 预览点餐页 / 后厨看单 / 大屏展示，均带 shopId 新标签页打开 | ✅ | — |
| 新手任务 ob-card | 金色欢迎礼行（体验期显示"首月进阶版价值 ¥99 已开通"）；开张四步：①上传第一道菜 ②装修店铺 ③预览点餐页（openCustomerPreview 并上报）④逛采购商城（switchTab 并上报）；"不再显示"存 localStorage `ob_hidden_<shopId>`；四步全部完成自动 claim-gift 领 500 鼎恒币并弹 obCelebrateModal 庆祝窗 | ✅ | ✅ |
| 升级引导弹窗 upsellModal | 见 6.6 节 | ✅ | ✅ |
| 手机框预览 customerPreviewModal | iframe 加载 customer.html?preview=1&embed=1 并叠加装修参数；Esc/✕ 关闭、点击遮罩不关闭；关闭时清空 iframe src | ✅ | ✅ <768px 隐藏 Esc 文字提示仅留 ✕ |
| 全局 | 左侧边栏 12 栏目；手机端 hamburger 按钮呼出侧边抽屉 + scrim 遮罩；所有请求经 api() 封装自动带 JWT；响应统一解析 data.data | ✅ | ✅ |

### 2.4 供应商后台（supplier-dashboard.html，逻辑内联）

> 入口 supplier-login.html（登录/注册 + 与商家页同款欢迎弹窗）。登录后先经"状态闸门"判定，通过后进入 6 栏目控制台。

| 页面/栏目 | 功能描述 | 状态 | 手机端适配 |
|---|---|---|---|
| 闸门页·审核中（status=pending） | 全屏遮罩 gateOverlay：⏳ 审核中、提交时间、审核进度（预计 1-2 个工作日）、"刷新审核状态"、退出登录、客服联系方式（📞 400-000-0000 占位 / 📧 support@dingheng.com 占位） | ✅（客服联系方式为占位 ❌） | ✅ |
| 闸门页·审核拒绝（status=rejected） | ✕ 入驻申请未通过、显示 rejectReason 拒绝原因、退出登录 | ✅ | ✅ |
| 闸门页·账户冻结（status=frozen） | ❄ 账户已被冻结、frozenReason 原因；明示在途订单可正常发货收货、新订单暂停接收、店铺已从采购商城隐藏 | ✅ | ✅ |
| 闸门页·协议签署（active 但未签协议） | 📜 签署合作协议：甲方（平台名取 PlatformConfig.platformCompanyName，未配置时显示占位文案）、乙方（供应商名自动带入）；协议正文 6 条（框架性占位文本，文中注明"正式条款将由平台后续替换"）；勾选"我已阅读并同意协议"后签署，POST /api/supplier/agreement/sign | 🟡 流程完整，协议正文为占位文本 | ✅ |
| 顶部接单状态横幅 orderBanner | 已开通接单：✅ 店铺已上线；资质核验中：⏳ 1-2 个工作日 + 刷新按钮；核验驳回：❌ 显示驳回原因 + "重新上传资质"；未上传：引导去"开通接单"栏目 | ✅ | ✅ |
| 待处理订单 orders | 今日新单/待确认数量；状态筛选 chips（待确认/已确认/已发货/已完成等）；订单表格/卡片；供应商确认订单（PUT confirm，冻结/未开通接单被拒）、发货（PUT/POST deliver、POST ship） | ✅（新单通知仅 console 模拟，webhook 为 TODO） | ✅ ≤768px order-body 单列 |
| 商品管理 products | 供应商品 CRUD（名称、分类、图片、单位默认"个"、成本价、市场价、库存、描述、上架/下架）；仅能操作自己的商品（supplierId 归属校验）；上架需 active+已签协议；空状态引导卡 supProdGuide（localStorage 按供应商记忆可关闭） | ✅ | ✅ |
| 开通接单 qualify | 资质核验：上传 4 张照片（营业执照 businessLicense、门店门头 storeFront、店内环境 storeInterior、货品照 goods，走 /api/supplier/upload）；提交后 qualification.status=pending；状态卡 none/pending/approved/rejected；pending/approved 状态表单只读锁定；导航栏"待办"角标（未开通且资质状态为 none/rejected 时显示） | ✅ | ✅ ≤560px 表单单列 |
| 通知设置 notify | 弹窗/短信/语音提醒开关（notificationSettings.popup/sms/voice）、webhookUrl、起送价 minOrderAmount（默认 300）；保存即标记 onboardNotifySet=true | 🟡 开关与 webhook 地址可存；短信/语音/webhook 无真实发送（console 模拟） | ✅ |
| 数据统计 stats | 今日新单数、待确认数、本月实付采购额（/api/supplier/stats） | 🟡 基础统计 | ✅ |
| 合作结算 settle | 本月分品类返点实时预估、当前所处档位、默认阶梯公示 defaultLadder（3%/5%/7%/9%/12%）、历史月度结算单列表（状态 待结算/已确认/已收款/已结算、超 15 天未收款标记）；点击"去看看"上报新手任务 settle 步 | ✅ | ✅ ≤768px 适配 |
| 新手任务 supObCard | "接单三步走"：①上架第一个商品 ②设置新订单提醒（保存通知设置即完成）③查看合作结算（点击上报）；全部完成显示"准备就绪"，无奖励 | ✅ | ✅ |

### 2.5 开发者控制台（dev-console.html，逻辑内联）

> 入口 dev-login.html（默认账号 admin / dingheng2024）。深色风格单页 8 栏目，全部接口 requireDev。

| 栏目 | 功能描述 | 状态 | 手机端适配 |
|---|---|---|---|
| 总览 overview | 平台统计卡（商家数、供应商数、本月已完成实付额）；待办入口（待审核注册、待核验资质，点击跳转供应商管理）；导航栏红点 supNavDot（待办数，读取 PlatformConfig.supplierTodoReadAt 计算） | ✅ | ✅ 含 768/560px 两档媒体查询，表格区 table-wrap 横向滚动 |
| 商家管理 merchants | 商家列表表格（GET /api/dev/merchants）；商家详情（GET /api/dev/merchants/:id，只读，含 previewUrl 预览链接） | ✅ 只读（无重置密码/编辑入口） | ✅ |
| 供应商管理 suppliers | 分页列表（aggregate，pending 置顶排序）；供应商详情（资质 4 图、商品列表、本月经营、近 12 月结算）；操作：审核通过 approve（→active）、拒绝 reject（需原因→rejected）、冻结 freeze（需原因→frozen）、解冻 unfreeze（→active）、开通接单 order-enable、资质通过 qualification/approve（4 图齐全时同时 orderEnabled=true）、资质驳回 qualification/reject（需原因）、删除供应商（级联删商品、保留历史订单）；供应商下拉 options | ✅ | ✅ |
| 返点中心 rebate | 专属返点规则 RebateRule 增删改查（按供应商+品类，tiers 支持 full 全额累进/marginal 超额分段、生效日期、启用开关）；返点概览 overview；手动月结按钮（POST /api/dev/rebate/settle）；月度结算单列表；确认结算 confirm、标记已收款 paid | ✅ | ✅ |
| 得币倍率 coinrules | CoinRule 品类倍率列表/新增/编辑（品类、倍率 0-10、启用开关、备注） | ✅ | ✅ |
| 对账中心 reconcile | 逐单返点核对：每单返点取值顺序 actualRebateAmount → preRebate → 分品类费率兜底计算；券成本统计；平台利润 = 返点收入 − 券成本 | ✅ | ✅ |
| 营销活动 marketing | 全平台所有商家营销活动只读列表 | ✅ 只读 | ✅ |
| 系统设置 system | 平台公司名称 platformCompanyName 配置（GET/PUT /api/dev/config，用于供应商协议甲方名称）；待办已读（POST /api/dev/todos/read） | ✅ | ✅ |

### 2.6 小程序端（uni-app/ 工程）

| 内容 | 状态 | 说明 |
|---|---|---|
| src/pages/admin/：index.vue（首页）、menu.vue（菜单）、order.vue（订单）、stats.vue（统计） | 🟡 | 商家端功能子集 |
| src/pages/customer/index.vue（顾客点餐） | 🟡 | 顾客端子集 |
| utils/request.js（请求封装）、cart.js（购物车）、audio.js（新单提示音，static/audio/new-order.wav） | 🟡 | 与 Web 端逻辑重复维护 |
| tabbar 图标（menu/order/shop/stats 各 2 态 png）、manifest.json/pages.json/theme.json/uni.scss | ✅ | 工程脚手架完整 |

---

## 3. 数据库（MongoDB，19 个集合）

> 多租户隔离：商家侧数据一律带字符串 `shopId`（格式 `shop_<时间戳>_<随机>`）；供应商侧带 ObjectId `supplierId`。所有模型均启用 timestamps（createdAt/updatedAt）。

### 3.1 账号与会员

**Admin（开发者账号）**
| 字段 | 类型 | 含义 |
|---|---|---|
| username | String, unique, required | 开发者用户名（默认 admin） |
| password | String, required | bcrypt 哈希（pre-save 钩子，已哈希 $2a/$2b/$2y 前缀跳过） |
| name | String | 姓名/昵称 |

**ShopAccount（商家登录账号）**
| 字段 | 类型 | 含义 |
|---|---|---|
| shopId | String, unique | 店铺标识，全系统多租户键 |
| shopName | String | 店铺名称 |
| contactName | String | 联系人姓名 |
| phone | String, unique(sparse) | 登录手机号 |
| password | String | bcrypt 哈希 |
| pointsBalance | Number, default 0 | 采购积分余额（收货时 $inc 累加，不触发 pre-save） |
| onboardPreview | Boolean | 新手任务：已预览点餐页 |
| onboardMallVisited | Boolean | 新手任务：已逛采购商城 |
| onboardGiftClaimed | Boolean | 新手任务 500 币礼是否已领 |

**Supplier（供应商账号）**
| 字段 | 类型 | 含义 |
|---|---|---|
| name | String | 供应商/店铺名称 |
| contact | String | 联系人 |
| phone | String | 联系电话 |
| loginAccount | String, unique | 登录账号（注册时取手机号小写） |
| password | String | bcrypt 哈希 |
| webhookUrl | String | 新订单回调地址（通知设置里可配，当前未真实调用） |
| categories | [String] | 经营品类标签 |
| minOrderAmount | Number, default 300 | 起送金额 |
| notificationSettings | Object | { popup, sms, voice } 三类提醒开关 |
| rebateRate | Number | 旧字段（返点已改由 RebateRule/默认阶梯计算） |
| balance | Number, default 0 | 返点余额（月结时 $inc） |
| status | String enum | pending 审核中 / active 已通过 / frozen 已冻结 / rejected 已拒绝，默认 pending |
| rejectReason | String | 审核拒绝原因 |
| frozenReason | String | 冻结原因 |
| approvedAt | Date | 审核通过时间 |
| agreementSigned | Boolean | 是否已签合作协议 |
| agreementSignedAt | Date | 协议签署时间 |
| orderEnabled | Boolean | 是否开通接单（资质核验通过后置 true） |
| orderEnabledAt | Date | 开通接单时间 |
| qualification | Object | 资质对象：status（none/pending/approved/rejected）、businessLicense、storeFront、storeInterior、goods（4 图 URL）、submittedAt、reviewedAt、rejectReason |
| onboardNotifySet | Boolean | 新手任务：已保存通知设置 |
| onboardSettleSeen | Boolean | 新手任务：已查看合作结算 |

**Member（商家会员档案，一店一条）**
| 字段 | 类型 | 含义 |
|---|---|---|
| shopId | String, unique | 店铺标识 |
| shopName | String | 店铺名冗余 |
| memberLevel | String enum | basic / advanced / premium，默认 basic |
| memberExpire | Date | 会员到期时间 |
| dinghengCoin | Number, default 0 | 鼎恒币当前余额 |
| totalEarnedCoin | Number, default 0 | 累计得币 |
| customerPointsEnabled | Boolean | 顾客积分功能开关（随会员等级） |
| memberIsTrial | Boolean | 是否体验期会员 |
| memberSource | String enum | coin 币兑换 / cash 现金 |
| pre-save 钩子 | — | 新文档自动赠送进阶版 30 天体验：memberLevel=advanced、memberIsTrial=true、memberExpire=now+30d、customerPointsEnabled=true |

### 3.2 门店业务

**Category（菜品分类）**：name(String)、shopId(String)；name+shopId 复合唯一。

**Dish（菜品）**
| 字段 | 类型 | 含义 |
|---|---|---|
| name | String | 菜名 |
| price | Number | 价格 |
| category | String | 分类名 |
| image | String | 图片 URL（/uploads/...） |
| isAvailable | Boolean, default true | 是否在售 |
| description | String | 描述 |
| shopId | String | 所属店铺 |

**Table（桌台）**：number（桌号）、shopId；number+shopId 复合唯一；status enum idle 空闲/occupied 占用（默认 idle）。

**Order（点餐订单）**
| 字段 | 类型 | 含义 |
|---|---|---|
| tableNumber | String | 桌号（积分兑换单固定为"积分兑换"） |
| items | [{dishName, price, quantity, category}] | 订单明细 |
| totalPrice | Number | 实付金额 |
| originalTotal | Number | 原价合计 |
| discountAmount | Number | 优惠总额 |
| discountDetail | Object | 优惠明细（营销折扣/满减/积分抵现/积分兑换等） |
| remark | String | 备注（≤200 字） |
| orderType | String enum | normal 普通单 / pointsRedeem 积分兑换单（0 元） |
| customerPhone | String | 顾客手机号（选填） |
| pointsUsed | Number | 本单抵现消耗积分 |
| pointsDiscount | Number | 积分抵现金额 |
| pointsEarned | Number | 本单累计积分 |
| status | String enum | pending 待处理 / completed 已完成，默认 pending |
| shopId | String | 所属店铺 |

**Setting（店铺设置，一店一条）**
| 字段 | 类型 | 含义/默认值 |
|---|---|---|
| shopId | String, unique | 店铺标识 |
| shopName | String, default '鼎恒餐饮' | 店名 |
| theme | String enum | classic/minimal/dark/green/redgold，默认 classic |
| bannerImage | String | 自定义横幅图（进阶版），空=主题默认 |
| logoImage | String | 自定义 LOGO（进阶版） |
| promoPoster | String | 优惠海报图（进阶版），空=文字轮播 |
| promoPosterSize | String enum | small/medium/large，默认 small |
| shopNameFont | String enum | modern/serif/round/hand，默认 modern |
| layout | String enum | list/large/grid，默认 list |
| enableVoice | Boolean, default true | 后厨语音播报开关 |
| enableBigscreen | Boolean, default true | 大屏开关 |
| enablePrinter | Boolean, default false | 打印开关 |
| enableWechat | Boolean, default false | 微信通知开关 |
| printerSN / printerKey | String | 打印机凭据（默认空） |
| notifyPhone | String | 通知手机号 |
| pointEnabled | Boolean, default true | 顾客积分总开关 |
| pointSpendPerPoint | Number, default 1, min 0 | 每消费 1 元返多少积分 |
| pointDeductEnabled | Boolean, default true | 积分抵现开关 |
| pointDeductPoints | Number, default 100, min 1 | 多少积分抵 1 元 |
| pointDeductMaxPercent | Number, default 20, 0-100 | 单笔抵现占实付上限百分比 |
| pointExchangeDishes | [{dishId(required), dishName, points(default 100, min 1)}] | 积分兑换菜品列表（后台最多配 20 个） |

**CustomerPoint（顾客积分账户）**
| 字段 | 类型 | 含义 |
|---|---|---|
| shopId + phone | 复合唯一 | 店铺 + 手机号 |
| points | Number, default 0 | 当前积分余额 |
| totalEarned | Number, default 0 | 累计获得积分 |
| history | [{type: earn/deduct/redeem, amount(正负), balance, orderId, note, time}] | 积分流水 |

**Marketing（营销活动）**
| 字段 | 类型 | 含义 |
|---|---|---|
| shopId / shopName | String | 所属店铺 |
| type | String enum | fullReduction 满减 / discount 折扣 / rechargeBonus 充值送 |
| threshold | Number | 满减门槛（满 X 元） |
| reduce | Number | 减免金额（减 Y 元） |
| rate | Number | 折扣率（如 0.95 = 95 折） |
| category | String | 折扣指定分类（折扣活动必填） |
| recharge | Number | 储值金额（充值送） |
| bonus | Number | 赠送金额（充值送） |
| title | String | 活动标题 |
| startTime / endTime | Date | 活动时间窗（endTime 可 null=长期） |
| enabled | Boolean, default false | 是否启用（新建默认停用） |

### 3.3 鼎恒币与券

**CoinHistory（鼎恒币流水/发币批次）**
| 字段 | 类型 | 含义 |
|---|---|---|
| shopId | String | 所属店铺 |
| amount | Number | 变动数额（正=发放，负=消耗/过期） |
| type | String enum | purchase_reward 采购奖励 / redeem_membership 兑换会员 / redeem_coupon 兑换券 / refund_deduct 退款扣回（枚举存在，当前无使用代码） / expired 过期 / system_gift 系统赠送 / new_shop_gift 新店礼 |
| sourceOrderId | ObjectId/String | 来源采购订单 |
| balanceAfter | Number | 变动后余额 |
| expireAt | Date | 批次过期时间（发币时 = 发放日 + 90 天） |
| remaining | Number | 该批次剩余可用币（FIFO 扣减/过期时更新） |
| isExpired | Boolean | 批次是否已过期 |
| description | String | 说明（采购奖励含品类倍率加成时注"含品类倍率加成"） |
| 索引 | shopId + createdAt | 流水查询 |

**CoinRule（品类得币倍率，平台级）**：category(String, unique)、multiplier(Number 0-10, default 1)、enabled(Boolean, default true)、remark(String)。标准品类：蔬菜/肉类/冻品/海鲜/酒水/粮油/调料/粮油酱料/一次性用品/其他。

**Coupon（抵用券，商家持有）**
| 字段 | 类型 | 含义 |
|---|---|---|
| shopId | String | 持有店铺 |
| type | String | 券类型标识 |
| name | String | 券名 |
| faceValue | Number | 面额（元） |
| minOrder | Number | 使用门槛（满 X 元可用） |
| status | String enum | unused 未使用 / used 已使用 / expired 已过期，默认 unused |
| usedOrderId | ObjectId | 核销的采购订单 |
| usedAt | Date | 核销时间 |
| expireDate | Date | 过期时间（发放 + 30 天） |

### 3.4 供应链

**SupplyProduct（供应商品）**
| 字段 | 类型 | 含义 |
|---|---|---|
| name | String | 商品名 |
| category | String | 品类 |
| image | String | 商品图 |
| unit | String, default '个' | 单位 |
| costPrice | Number | 成本价 |
| marketPrice | Number | 市场价 |
| rebateRate | Number | 旧字段（商家视图 toMerchantView 已删除该字段，返点以 RebateRule 为准） |
| supplierId | ObjectId ref Supplier | 所属供应商 |
| supplierName | String | 供应商名冗余 |
| stock | Number | 库存 |
| status | String enum | 上架 / 下架，默认上架 |
| description | String | 描述 |

**PurchaseOrder（采购订单）**
| 字段 | 类型 | 含义 |
|---|---|---|
| orderNo | String, unique | 订单号（CG + 年月日 + 4 位随机） |
| shopId / shopName | String | 采购商家 |
| supplierId | ObjectId ref Supplier | 供货供应商 |
| items | [{productId ref SupplyProduct, name, category, quantity, unitPrice, totalPrice}] | 采购明细 |
| totalAmount | Number | 订单总额 |
| status | String enum | 待确认 / 已确认 / 已发货 / 已收货 / 已完成（实际流转：待确认→已确认→已发货→已完成，收货即完成） |
| confirmAt / deliverAt / receiveAt | Date | 确认/发货/收货时间 |
| rebateAmount | Number | 旧返点字段 |
| preRebate | Number | 收货时预估返点金额 |
| preRebateRate | Number | 预估时使用费率 |
| actualRebateAmount | Number | 月结时回写的实际返点 |
| rebateMonth | String | 返点归属月份（YYYY-MM，按 receiveAt 无则 createdAt） |
| rebateSettled | Boolean | 是否已月结 |
| pointsGenerated | Number | 本单产生的商家采购积分（=totalAmount，收货时 $inc ShopAccount.pointsBalance） |
| rewardCoin | Number | 本单发放鼎恒币（≠0 防重复发放） |
| appliedCouponId | ObjectId ref Coupon | 使用的抵用券 |
| discountAmount | Number | 券抵扣金额 |
| actualPayAmount | Number | 实付金额 |

**RebateRule（专属返点规则）**：supplierId(ObjectId ref) + category(String, default '全部') 复合唯一；tiers[{minAmount 门槛, rate 费率 0-1, mode: full 全额累进/marginal 超额分段}]；enabled(Boolean default true)；effectiveDate(Date)；remark(String)。

**RebateSettlement（月度返点结算单）**
| 字段 | 类型 | 含义 |
|---|---|---|
| month + supplierId | 复合唯一 | 结算月份（YYYY-MM）+ 供应商 |
| supplierName | String | 供应商名冗余 |
| totalPurchaseAmount | Number | 当月采购总额 |
| totalRebateAmount | Number | 当月返点总额 |
| details | [{category, purchaseAmount, rebateAmount, rate, tierMinAmount, mode: full/marginal/default}] | 分品类结算明细 |
| status | String enum | 待结算 / 已确认 / 已收款 / 已结算（normalizeSettlementStatus 将"已结算"归一为"已确认"） |
| settledAt / confirmedAt / paidAt | Date | 结算/确认/收款时间 |
| remark | String | 备注 |

**PlatformConfig（平台配置单例）**：key(String, unique, 固定 'platform')、platformCompanyName(String 平台公司全称，用于协议甲方)、supplierTodoReadAt(Date 供应商待办红点已读时间)。

### 3.5 集合间关系

```
ShopAccount.shopId ──┬── Member.shopId（一一会员档案）
                     ├── Setting.shopId（一一设置）
                     ├── Category/Dish/Table/Order.shopId（一对多）
                     ├── CoinHistory.shopId / Coupon.shopId（一对多）
                     ├── CustomerPoint.shopId（一对多，按 phone 区分顾客）
                     └── Marketing.shopId / PurchaseOrder.shopId（一对多）

Supplier._id ──┬── SupplyProduct.supplierId（一对多）
               ├── PurchaseOrder.supplierId（一对多）
               ├── RebateRule.supplierId（一对多，按 category 区分）
               └── RebateSettlement.supplierId（一对多，按 month 区分）

PurchaseOrder.items.productId ──→ SupplyProduct._id
PurchaseOrder.appliedCouponId ──→ Coupon._id；Coupon.usedOrderId ──→ PurchaseOrder._id
CoinHistory.sourceOrderId ──→ PurchaseOrder._id（采购返币/消耗）
Order（点餐单）.shopId ──→ Setting/Member；CustomerPoint.history.orderId ──→ Order._id
Admin / PlatformConfig / CoinRule：平台级，无 shopId
```

---

## 4. API 路由全表

> 统一响应格式 `{ success: true, data: ... }`；错误 `{ success: false, message }`。
> 鉴权说明：**公开(shopId)** = 需经 requirePublicShopId（x-shop-id 头 / query / body 传 shopId）；**商家** = requireMerchant（JWT role=merchant 且 shopId 一致）；**供应商** = requireSupplier（JWT role=supplier 且 supplierId 一致）；**开发者** = requireDev（JWT role=dev）；**登录态** = verifyToken 任意角色。

### 4.1 认证（routes/auth.js，挂载 /api/auth）

| 方法 | 路径 | 用途 | 身份 |
|---|---|---|---|
| POST | /api/auth/merchant/register | 商家注册（自动建 Setting + 5 默认分类 + Member 体验会员） | 公开 |
| POST | /api/auth/merchant/login | 商家登录（手机号+密码，发 JWT） | 公开 |
| POST | /api/auth/supplier/register | 供应商注册（status=pending，loginAccount=手机号小写） | 公开 |
| POST | /api/auth/supplier/login | 供应商登录 | 公开 |
| POST | /api/auth/dev/login | 开发者登录 | 公开 |
| POST | /api/auth/dev/seed | 种子数据 | 公开 |
| POST | /api/auth/merchant/change-password | 商家改密（校验旧密码，新密码≥6位） | 商家 |
| POST | /api/auth/supplier/change-password | 供应商改密 | 供应商 |
| POST | /api/auth/dev/change-password | 开发者改密 | 开发者 |

### 4.2 公共/顾客侧（server.js 内联）

| 方法 | 路径 | 用途 | 身份 |
|---|---|---|---|
| POST | /api/admin/upload | 菜品/装修图片上传（multer，2MB，jpg/png） | 商家 |
| POST | /api/supplier/upload | 资质图片上传 | 供应商 |
| GET | /api/categories | 店铺分类列表 | 公开(shopId) |
| GET | /api/dishes | 店铺在售菜品 | 公开(shopId) |
| GET | /api/tables | 桌台列表 | 公开(shopId) |
| POST | /api/orders | 顾客下单（营销 computeDiscount + 积分抵现 + settlePointsForOrder + 通知引擎：语音/大屏/打印/微信） | 公开(shopId) |
| GET | /api/orders | 订单查询（后厨轮询 ?status=pending） | 公开(shopId) |
| PUT | /api/orders/:id/complete | 订单完成出餐 | 公开(shopId) |
| GET | /api/settings | 店铺公开设置（主题/店名/积分配置等） | 公开(shopId) |
| GET | /api/admin/stats | 门店统计（后厨/大屏用） | 公开(shopId) |
| GET | /api/suppliers | 公开供应商列表（仅 active+agreementSigned+orderEnabled） | 公开 |
| POST | /api/suppliers | 供应商注册（server.js 内联，无鉴权） | 公开 |
| GET | /api/shop-accounts/:shopId | 查商家账号公开信息 | 公开 |

### 4.3 商家后台（server.js 内联）

| 方法 | 路径 | 用途 | 身份 |
|---|---|---|---|
| GET/POST | /api/admin/categories | 分类列表/新增 | 商家 |
| PUT/DELETE | /api/admin/categories/:id | 分类编辑/删除 | 商家 |
| GET/POST | /api/admin/dishes | 菜品列表/新增 | 商家 |
| PUT/DELETE | /api/admin/dishes/:id | 菜品编辑/删除 | 商家 |
| GET/POST | /api/admin/tables | 桌台列表/新增 | 商家 |
| DELETE | /api/admin/tables/:id | 桌台删除（无 PUT 编辑） | 商家 |
| GET | /api/admin/orders | 商家视角订单列表 | 商家 |
| GET | /api/admin/merchant-stats | 商家营业统计 | 商家 |
| GET | /api/admin/onboarding | 新手任务状态 | 商家 |
| POST | /api/admin/onboarding/step | 上报新手任务步骤 | 商家 |
| POST | /api/admin/onboarding/claim-gift | 领取新手 500 鼎恒币 | 商家 |
| PUT | /api/settings | 保存店铺设置（主题/图片/积分字段校验；PREMIUM_THEMES 与图片需进阶版） | 商家 |

### 4.4 鼎恒币/会员（routes/coin.js，挂载 /api）

| 方法 | 路径 | 用途 | 身份 |
|---|---|---|---|
| POST | /api/coin/exchange-membership | 币兑换会员月卡（四分支：同级续费/升级折算/降级拒绝；事务 + FIFO 扣币） | 无鉴权中间件，body 传 shopId（前端登录后调用） |
| POST | /api/coin/exchange-coupon | 币兑换抵用券（等级+余额校验，券 30 天有效） | 同上 |
| GET | /api/coin/category-multipliers | 品类倍率公开表 | 公开 |
| GET | /api/coin/month-progress/:shopId | 本月采购得币 + 最近兑换目标进度 | 公开 |
| GET | /api/coin/status/:shopId | 币余额/等级/到期/体验期标记/未用券/7 天内到期券 | 公开 |
| GET | /api/member/permissions/:shopId | 会员等级与功能权益矩阵 | 公开 |
| GET | /api/coin/history/:shopId | 币流水列表 | 公开 |

### 4.5 采购（routes/purchaseOrders.js，挂载 /api/purchase-orders）

| 方法 | 路径 | 用途 | 身份 |
|---|---|---|---|
| GET | /api/purchase-orders | 订单列表（商家→自己 shopId；dev→全部；带 populate） | 登录态 |
| GET | /api/purchase-orders/supplier | 供应商视角订单列表 | 供应商 |
| POST | /api/purchase-orders | 商家下单（校验供应商 active+协议+接单、起送价、商品归属、券校验；事务内建单+核销券） | 商家 |
| PUT/POST | /api/purchase-orders/:id/confirm | 供应商确认订单（冻结/未开通接单拒绝） | 供应商 |
| PUT/POST | /api/purchase-orders/:id/deliver；POST /:id/ship | 供应商发货 | 供应商 |
| POST | /api/purchase-orders/:id/confirm-receive；PUT /:id/receive | 商家确认收货（已发货→已完成；发鼎恒币 calcOrderRewardCoin、采购积分 $inc、预估返点 preRebate、写 purchase_reward 流水 90 天有效） | 商家 |
| POST | /api/purchase-orders/:id/apply-coupon | 订单试算/应用抵用券 | 商家 |

### 4.6 供应商（routes/supplier.js，挂载 /api/supplier）

| 方法 | 路径 | 用途 | 身份 |
|---|---|---|---|
| GET | /api/supplier/profile | 供应商档案（含 platformCompanyName、治理状态、资质） | 供应商 |
| POST | /api/supplier/qualification/submit | 提交 4 张资质（需 active+已签协议；URL 须 /uploads/ 或 http(s)） | 供应商 |
| POST | /api/supplier/agreement/sign | 签署合作协议（需 active + agree=true） | 供应商 |
| GET | /api/supplier/orders/pending | 今日待确认订单 | 供应商 |
| GET | /api/supplier/stats | 今日新单/待确认/本月实付额 | 供应商 |
| GET/PUT | /api/supplier/notification-settings；GET/PUT/PATCH /api/supplier/settings | 通知设置读写（保存即 onboardNotifySet=true） | 供应商 |
| GET | /api/supplier/settle | 本月分品类返点预估+当前档位+默认阶梯+历史结算 | 供应商 |
| GET | /api/supplier/onboarding | 新手三步走状态 | 供应商 |
| POST | /api/supplier/onboarding/step | 上报新手步骤（仅 settle 步） | 供应商 |

### 4.7 供应商品（routes/supplyProducts.js，挂载 /api/supply-products）

| 方法 | 路径 | 用途 | 身份 |
|---|---|---|---|
| GET | /api/supply-products | 公开商品目录（仅上架 + 供应商 active/已签协议/已接单；toMerchantView 删 rebateRate、加 coinMultiplier） | 公开 |
| GET | /api/supply-products/mine | 供应商自己的全部商品 | 供应商 |
| GET | /api/supply-products/:id | 商品详情 | 公开 |
| POST | /api/supply-products | 新增商品（需 active+已签协议） | 供应商 |
| PUT/DELETE | /api/supply-products/:id | 编辑/删除（supplierId 归属校验） | 供应商 |

### 4.8 营销（routes/marketing.js，挂载 /api/marketing）

| 方法 | 路径 | 用途 | 身份 |
|---|---|---|---|
| GET | /api/marketing | 商家自己的活动列表 | 商家 |
| POST | /api/marketing | 新建活动（满减需进阶版；折扣需尊享版且必须指定分类；充值送需尊享版；默认 enabled=false） | 商家 |
| PUT | /api/marketing/:id | 编辑活动（等级校验同上） | 商家 |
| PATCH | /api/marketing/:id/toggle | 启用/停用切换 | 商家 |
| DELETE | /api/marketing/:id | 删除活动 | 商家 |
| GET | /api/marketing/active | 当前时间窗内启用的活动（满减/折扣/充值送） | 公开(shopId) |

### 4.9 顾客积分（routes/customerPoints.js，挂载 /api/points）

| 方法 | 路径 | 用途 | 身份 |
|---|---|---|---|
| GET | /api/points/config | 积分配置（enabled/返积分比例/抵现规则/兑换菜列表） | 公开(shopId) |
| POST | /api/points/query | 按手机号查积分余额（/^1\d{10}$/ 校验） | 公开(shopId) |
| POST | /api/points/redeem-dish | 积分换菜（校验菜品在售+配置；findOneAndUpdate 原子扣积分防超扣；生成 orderType=pointsRedeem 的 0 元 pending 订单进后厨） | 公开(shopId) |

### 4.10 开发者（routes/dev.js，挂载 /api/dev，全部 requireDev）

| 方法 | 路径 | 用途 |
|---|---|---|
| GET | /api/dev/stats | 平台总览（商家数/供应商数/本月已完成实付额） |
| GET | /api/dev/merchants | 商家列表 |
| GET | /api/dev/merchants/:id | 商家详情（只读 + previewUrl） |
| GET | /api/dev/suppliers | 供应商分页列表（pending 置顶） |
| GET | /api/dev/suppliers/options | 供应商下拉选项 |
| GET | /api/dev/suppliers/:id/detail | 供应商详情（资质 4 图/商品/本月经营/近 12 月结算） |
| PUT | /api/dev/suppliers/:id/approve | 审核通过（→active） |
| PUT | /api/dev/suppliers/:id/reject | 审核拒绝（需原因，→rejected） |
| PUT | /api/dev/suppliers/:id/freeze | 冻结（需原因，→frozen） |
| PUT | /api/dev/suppliers/:id/unfreeze | 解冻（→active） |
| PUT | /api/dev/suppliers/:id/order-enable | 单独开通接单 |
| PUT | /api/dev/suppliers/:id/qualification/approve | 资质通过（4 图齐全→orderEnabled=true） |
| PUT | /api/dev/suppliers/:id/qualification/reject | 资质驳回（需原因） |
| DELETE | /api/dev/suppliers/:id | 删除供应商（级联删商品，保留订单） |
| GET | /api/dev/todos | 待办数（待审核/待核验，按 supplierTodoReadAt） |
| POST | /api/dev/todos/read | 待办已读（写 supplierTodoReadAt） |
| GET/PUT | /api/dev/config | 平台配置读写（platformCompanyName） |
| GET | /api/dev/reconciliation | 对账中心（逐单返点 actual→pre→分品类兜底；券成本；平台利润） |
| GET/POST | /api/dev/rebate/rules | 返点规则列表/新增 |
| PUT/DELETE | /api/dev/rebate/rules/:id | 返点规则编辑/删除 |
| GET | /api/dev/rebate/overview | 返点概览 |
| POST | /api/dev/rebate/settle | 手动触发月结 |
| GET/POST | /api/dev/coin-rules | 得币倍率列表/新增（倍率 0-10 校验） |
| GET | /api/dev/rebate/settlements | 结算单列表 |
| PUT | /api/dev/rebate/settlements/:id/confirm | 结算单确认（待结算→已确认） |
| PUT | /api/dev/rebate/settlements/:id/paid | 标记已收款（→已收款） |
| GET | /api/dev/marketing | 全平台营销活动只读列表 |

---

## 5. 商业规则实现现状

### 5.1 会员三档权益与权限控制点

**定价与币价（utils/dhConfig.js）**

| 档位 | 人民币月价 | 鼎恒币月价 | 定位 |
|---|---|---|---|
| basic 基础版 | ¥0 | — | 注册默认开通 |
| advanced 进阶版 | ¥99 | 3000 币 | 热销 |
| premium 尊享版 | ¥199 | 5000 币 | 旗舰 |

- 新商家注册：Member pre-save 自动赠送进阶版 30 天体验（memberIsTrial=true）。
- 体验/会员到期：每日 02:00 定时任务降级为 basic。

**功能权益矩阵（dhConfig.features）**

| 功能权益 | basic | advanced | premium | 控制点 |
|---|---|---|---|---|
| 顾客积分 customerPoints | ✗ | ✓ | ✓ | getPointConfig 综合判定（等级 + pointEnabled + 比例>0）；后台积分栏锁定遮罩 |
| 满减活动 marketingDiscount | ✗ | ✓ | ✓ | routes/marketing.js 新建/编辑校验；营销栏满减卡 |
| 分类折扣 marketingCategoryDiscount | ✗ | ✗ | ✓ | 同上，折扣活动必须指定分类；折扣卡内联锁 |
| 充值送 marketingRecharge | ✗ | ✗ | ✓ | 同上；充值送卡（储值账户未接入） |
| marketingFull | ✗ | ✗ | ✓ | 权限位存在，无独立功能页面 |
| 基础报表 reportBasic | ✗ | ✓ | ✓ | 权限位存在，统计栏仅今日/本月营业额 |
| 高级报表 reportAdvanced | ✗ | ✗ | ✓ | 权限位存在，无对应页面 |
| 高级主题（dark/green/redgold） | ✗ | ✓ | ✓ | server.js PUT /api/settings：PREMIUM_THEMES=['dark','green','redgold'] 非进阶版拒绝写入 |
| 自定义图片（横幅/LOGO/海报） | ✗ | ✓ | ✓ | server.js：IMAGE_NEED_LEVEL='advanced' 校验 |
| 采购得币汇率 coinRate | 0.5 币/元 | 1 币/元 | 1 币/元 | calcOrderRewardCoin 按 memberLevel 取费率 |
| 抵用券兑换 | 仅前 3 档券 | 前 5 档券 | 全部 6 档 | exchange-coupon 等级校验 |

其他控制点：middlewares/featureCheck.js 的 checkFeature(featureName)；前端 goUpgrade 统一升级引导。

### 5.2 鼎恒币全套规则

**发放**

- 来源 1：采购订单确认收货时发放（routes/purchaseOrders.js → utils/coinRule.js calcOrderRewardCoin）：
  - 逐行计算：行实付 = 行原价 × (actualPayAmount / totalAmount) 分摊；行币 = 行实付 × 会员币汇率（basic 0.5 / advanced 1 / premium 1）× 品类倍率；汇总后 floor。
  - 品类倍率取 CoinRule（enabled）：精确品类匹配 → "其他"兜底 → 默认 1；倍率范围 0-10。
  - 返回 { rewardCoin, baseCoin, boosted, lines }；写 CoinHistory（type=purchase_reward，expireAt=发放+90 天，remaining=发币额；倍率>1 时 description 注"含品类倍率加成"）。
  - rewardCoin≠0 防重复发放；同时 ShopAccount.pointsBalance $inc totalAmount（采购积分）。
- 来源 2：新手任务礼——商家四步任务完成后 claim-gift 发 500 币（new_shop_gift/system_gift 流水）。
- 开发者控制台可维护 CoinRule 品类倍率；标准品类：蔬菜/肉类/冻品/海鲜/酒水/粮油/调料/粮油酱料/一次性用品/其他。

**倍率**：CoinRule 集合按品类存储（category 唯一、multiplier 0-10、enabled、remark），不硬编码于业务代码；商家端商品视图展示 coinMultiplier 但不展示返点费率。

**过期**：coinExpireDays=90；每日 02:00 clearExpiredCoins 按发币批次处理：到期批次 remaining→0、isExpired=true，Member.dinghengCoin 扣减（不足扣至 0），写 expired 流水。

**FIFO 扣减**：兑换时 deductCoinsFifo 按 CoinHistory 批次 createdAt 升序，逐批扣 remaining 至扣满为止；事务（mongoose session）执行。

**兑换会员（exchange-membership，四分支）**

1. 同级续费：有效期顺延 30 天；
2. basic → advanced/premium：直接扣 3000/5000 币，新效期 = 今天 + 30 天；
3. advanced → premium：剩余天数按日成本折算（DAILY_COIN.advanced=100/天、premium=167/天）：proratedValue = 剩余天数 × 100，需补扣 5000 − proratedValue，新效期 = 30 天 + floor(proratedValue / 167) 天；
4. premium → advanced：拒绝（403，"尊享版无法降兑为进阶版"）。

**兑换抵用券**：等级 + 余额校验，扣币发券，券 expireDate = 今天 + 30 天（couponExpireDays=30）。

**币状态查询**：/api/coin/status 返回余额、等级、到期、体验期标记、未使用券、7 天内到期券；/api/coin/month-progress 返回本月采购得币与最近兑换目标进度。

### 5.3 抵用券面额与定价表（dhConfig.coupons）

| 面额 | 鼎恒币价格 | 使用门槛 | 最低会员等级 |
|---|---|---|---|
| 10 元 | 600 币 | 满 300 元 | basic |
| 20 元 | 1100 币 | 满 500 元 | basic |
| 30 元 | 1500 币 | 满 800 元 | basic |
| 50 元 | 2400 币 | 满 1200 元 | advanced |
| 100 元 | 4500 币 | 满 2500 元 | advanced |
| 200 元 | 8000 币 | 满 4000 元 | premium |

- 券有效期 30 天；每日 02:00 过期任务将 unused → expired。
- 采购下单时可选用券（车中选择 → apply-coupon 试算 → 下单事务内核销：status=used、usedOrderId、usedAt）；订单记录 appliedCouponId、discountAmount、actualPayAmount。
- 对账中心将券成本计入平台利润核算（利润 = 返点 − 券成本）。

### 5.4 阶梯返点

**默认规则（utils/rebate.js DEFAULT_TIERS，全额累进 full）**

| 月采购额区间 | 费率 |
|---|---|
| 0 – 1 万 | 3% |
| 1 万 – 5 万 | 5% |
| 5 万 – 15 万 | 7% |
| 15 万 – 50 万 | 9% |
| 50 万以上 | 12% |

- DEFAULT_REBATE_RATE=0.03（兜底费率）；ALL_CATEGORY='全部'；建议品类：肉类/蔬菜/粮油/酒水/冻品/调料/其他。
- 供应商后台"合作结算"页公示默认阶梯（defaultLadder）。

**RebateRule 结构**：按供应商 + 品类配置（品类默认"全部"，复合唯一）；tiers 数组每项 { minAmount 门槛, rate 费率 0-1, mode }：
- `full` 全额累进：月采购额达到某档后，全额按该档费率计；
- `marginal` 超额分段：仅超出门槛部分按该档费率（marginalRateAt 取边际费率）；
- 无专属规则或规则未启用时走默认阶梯（明细 mode 标记 default）。
- 规则含 enabled 开关、effectiveDate 生效日期、remark；开发者控制台增删改查。

**订单预估（estimateOrderRebate）**：采购单确认收货时，按"本单分品类采购额 × 当月累计采购额定档后的边际费率"预估，写 preRebate/preRebateRate。

**月结流程（settleMonth）**：
- 触发：每月 1 号 03:00 定时任务，或开发者控制台手动 POST /api/dev/rebate/settle；幂等（已结算月份/订单不重复）。
- 月份归属：按订单 receiveAt（无则 createdAt）。
- 每个供应商一个事务：创建 RebateSettlement（status=待结算，details 分品类列示 purchaseAmount/rebateAmount/rate/tierMinAmount/mode）；回写订单 actualRebateAmount、rebateSettled=true、rebateMonth；Supplier.balance $inc 返点总额；无事务环境时降级为非事务执行。
- 结算单状态流：待结算 →（开发者 confirm）已确认 →（开发者 paid）已收款；normalizeSettlementStatus 将历史"已结算"归一为"已确认"。
- 超期标记：SETTLEMENT_OVERDUE_DAYS=15，settledAt 后超 15 天未收款标记逾期（供应商结算页可见）。
- 对账中心：逐单返点取值 actualRebateAmount → preRebate → 分品类费率兜底重算。

### 5.5 供应商治理流程（状态机）

```
注册(POST /api/suppliers，无鉴权)
   │  status = pending
   ▼
开发者审核（dev-console 供应商管理）
   ├── reject（需原因）→ status = rejected（终态页：显示原因，仅退出登录）
   └── approve → status = active，approvedAt
                    │
                    ▼
          签署合作协议（supplier-dashboard 闸门页，POST /agreement/sign，需 active + agree=true）
                    │  agreementSigned = true，agreementSignedAt
                    ▼
          进入控制台（可浏览/上架商品/看结算；商城仍不可见）
                    │
          上传 4 张资质（POST /qualification/submit，需 active+协议；
          营业执照/门头/店内/货品，URL 必须 /uploads/ 或 http(s)）
                    │  qualification.status = pending，submittedAt
                    ▼
          开发者资质核验
          ├── qualification/reject（需原因）→ qualification.status = rejected（顶部横幅引导重新上传，可重复提交）
          └── qualification/approve（校验 4 图齐全）→ qualification.status = approved
                    │  同时 orderEnabled = true，orderEnabledAt
                    ▼
          店铺上线采购商城（GET /api/suppliers、GET /api/supply-products 可见；可接新单）
```

- 冻结/解冻：开发者 freeze（需 frozenReason）→ status=frozen：在途订单可继续发货/收货，新订单确认接口拒绝，店铺从商城隐藏；unfreeze → active。
- 开发者另有 order-enable 接口可单独开关接单；删除供应商时级联删除商品、保留历史订单。
- 商家下单校验：供应商必须 active + agreementSigned + orderEnabled；满足起送价 minOrderAmount（默认 300）；商品必须归属该供应商。
- 供应商操作采购单需 JWT supplierId 与订单 supplierId 一致。
- 待办红点：PlatformConfig.supplierTodoReadAt；dev /todos 返回待审核注册 + 待核验资质数量；供应商导航栏"开通接单"角标在未开通且资质 none/rejected 时显示。

### 5.6 顾客积分规则（进阶版权益）

- **功能开关**：Setting.pointEnabled（默认 true）+ 会员等级 ∈ [advanced, premium] + pointSpendPerPoint > 0，三者同时满足才 enabled（getPointConfig）。
- **返积分比例**：pointSpendPerPoint = 每消费 1 元返多少积分（默认 1，即 1 元=1 积分；0=不返）。
- **累计**：下单成功 settlePointsForOrder：earn = floor(实付金额 × pointSpendPerPoint)；先写抵现扣减（deduct）再写获得（earn）；CustomerPoint.history 记录 type/amount/balance/orderId/note/time。
- **抵现**：pointDeductEnabled（默认 true）；pointDeductPoints = 多少积分抵 1 元（默认 100）；pointDeductMaxPercent = 单笔最多抵实付的百分比（默认 20%）；顾客结算页勾选使用，前后端同规则计算；下单扣减写 history（note="下单抵现"）。
- **积分换菜**：商家在后台从自家菜单选菜配置所需积分（pointExchangeDishes，默认 300 积分，最多 20 个）；顾客在点餐页"积分换菜"入口/成功页链接进入，手机号查余额后兑换；redeem-dish 原子扣减（findOneAndUpdate 条件 points≥所需，防并发超扣），生成 orderType=pointsRedeem 的 0 元订单（tableNumber="积分兑换"，备注"积分兑换菜品，凭本单出餐（X 积分）"）进入后厨 pending 队列。
- **手机号**：大陆 11 位 /^1\d{10}$/；顾客选填，localStorage 记忆；CustomerPoint 以 shopId+phone 复合唯一。
- **顾客侧展示**：每菜"可得 X 积分"小字；优惠条积分规则卡；结算页"本单可得 X 积分"+抵现选项；成功页积分播报与可兑换菜链接。

### 5.7 营销活动类型

| 类型 | type | 权益等级 | 规则 | 下单是否参与计价 |
|---|---|---|---|---|
| 满减 | fullReduction | 进阶版+ | threshold 满 X 元、reduce 减 Y 元；可建多条，下单时折扣后小计满足门槛取减免最大者（bestFull） | ✅ 参与 |
| 分类折扣 | discount | 尊享版 | 必须指定 category；rate 折扣率；同一商品分类折扣优先于全场折扣取最低 rate（后端强制折扣必须选分类，无全场折扣选项） | ✅ 参与 |
| 充值送 | rechargeBonus | 尊享版 | recharge 储值额、bonus 赠送额；活动可创建/展示，优惠条可见 | ❌ 不参与（储值账户体系未接入，卡片注明"储值账户体系后续接入"） |

- 活动字段：title、startTime、endTime（可 null=长期）、enabled（新建默认 false，需手动启用）。
- 计价顺序（utils/marketingCalc.js computeDiscount）：先折扣（分类/全场取最低）→ 后满减（折扣后小计达门槛取最大减免）；返回 originalTotal/itemDiscountAmount/fullReductionAmount/finalTotal/bestFull/globalRate/catRates；点餐页前端内嵌同规则用于购物车实时计算。
- 公开接口 GET /api/marketing/active 仅返回当前时间窗内 enabled 的活动（满减/折扣/充值送）。

---

## 6. 关键前端机制

### 6.1 主题系统（5 主题 × 3 排版 × 4 字体）

- **5 主题**（customer.html body.theme-*，CSS 变量定义于 :root 与 body.theme-*）：
  - classic 经典橙（免费）、minimal 简约白（免费）；
  - dark 时尚暗黑（深暖棕黑 #1B1613 + 琥珀橙 #FF9F45 + 暖白 #F5EFE6）、green 清新绿、redgold 国潮红金（后三者进阶版锁定）。
  - admin 装修栏 THEME_PREVIEW/SHOP_THEMES 配色与顾客端变量保持一致。
- **3 排版**（body.layout-*）：list 经典列表（默认）、large 大图模式、grid 双列网格；URL 参数 layout=list/large/grid 可覆盖（预览/外链用）。
- **4 字体**（Setting.shopNameFont，顾客端 --font-shopname）：modern 现代黑体系、serif 雅致宋体系、round 圆润体、hand 手写/楷体风格（NAME_FONT_STACKS 系统字体栈实现）。
- **自定义图片**：bannerImage（横幅，顾客端以 --banner-layer 加遮罩渲染）、logoImage（店名左侧显隐）、promoPoster（优惠海报）；均进阶版权益，空值回退主题默认。
- 历史约束：--banner-grad 必须为合法渐变 background-image；纯色值会导致声明失效回退背景色；新增按钮类主题色需每套主题都定义变量。

### 6.2 预览机制

- 商家后台装修栏右侧 sticky 手机框：iframe 加载 `customer.html?preview=1&embed=1&shopId=...`，并追加 URL 参数 theme、shopNameFont/font、layout、bannerImage、logoImage、shopName。
- 顾客端 getPreviewOverrides() 读取这些 URL 参数，在 loadShopInfo 后覆盖设置（不写库）。
- 预览行为与真实点餐页对齐：cartBar 购物车栏正常显示（仅 table-pill 桌号牌隐藏）；ckSubmitBtn 在 preview=1 时拦截下单并 toast，防止预览产生真实订单。
- 预览弹窗 customerPreviewModal：Esc 键或 ✕ 按钮关闭，点击遮罩不关闭；关闭时清空 iframe src；<768px 手机端隐藏 Esc 文字提示仅留 ✕；embed 模式顾客端显示 backNav 返回按钮。
- 外链"预览点餐页"工具同样带 preview=1 新标签打开。

### 6.3 欢迎弹窗（登录页）

- merchant-login.html 与 supplier-login.html 均有：首次访问页面 300ms 后弹出 welcome-overlay（"欢迎加入鼎恒科技"，商家页注"注册即送首月进阶版会员（价值 ¥99）"）。
- 关闭方式：✕、"立即免费使用"按钮（切到注册 Tab）、点击遮罩。
- 关闭时间戳写 localStorage（merchant_welcome_closed_at 等），7 天内不再显示。

### 6.4 新手任务清单

**商家·开张四步（admin.html ob-card + /api/admin/onboarding）**
1. 上传第一道菜（dish：后端检测菜品数量）；
2. 装修店铺（decorate：应用主题/装修设置）；
3. 预览点餐页（preview：打开手机框预览即上报 step）；
4. 逛采购商城（mall：切换到商城栏即上报）。
- 金色欢迎礼行：体验期显示"首月进阶版价值 ¥99 已开通"。
- "不再显示"复选框写 localStorage `ob_hidden_<shopId>`。
- 四步全部完成：自动 POST claim-gift 领取 500 鼎恒币，弹 obCelebrateModal 庆祝窗。

**供应商·接单三步走（supplier-dashboard.html supObCard + /api/supplier/onboarding）**
1. 上架第一个商品（product：后端检测上架商品数）；
2. 设置新订单提醒（notify：保存一次通知设置即完成，onboardNotifySet）；
3. 查看合作结算（settle：点击"去看看"上报，onboardSettleSeen）。
- 全部完成显示"准备就绪"，无物质奖励。

### 6.5 引导卡

- 采购商城空状态引导卡 mallGuide：无供应商/未逛商城时展示，localStorage 记忆关闭。
- 供应商商品空状态引导卡 supProdGuide：无商品时展示，localStorage 键 `guide_dismiss_supplier_<id>_products`。
- 采购激励进度卡 mallProgress：/api/coin/month-progress 展示本月得币与兑换目标进度。
- 等级差异化横幅 mallCoinBanner：按会员等级展示不同采购得币引导文案。

### 6.6 升级跳转与弹窗逻辑

- **触发点**：点击任何锁定元素（高级主题、图片上传、积分栏、营销折扣/充值送卡等）→ toast 提示权限不足 + 调用 goUpgrade(target)。
- **goUpgrade**：切换到会员中心栏；目标人民币卡（vplanAdvanced/vplanPremium）添加 upgrade-glow 呼吸高亮 2 秒；随后执行 checkUpsellAfterUpgrade。
- **升级引导弹窗 upsellModal**：checkUpsellAfterUpgrade 先 waitForCoinLoaded（等待 ezCoinBalance 余额加载完成，dataset.loaded），若币余额 ≥ 目标档币价（进阶 3000/尊享 5000）则 showUpsellModal；同一次访问只弹一次（_upsellShownThisVisit）；余额不足不弹。
  - 文案："💡 发现你有 X 鼎恒币，可直接免费兑换进阶版/尊享版月卡（价值 ¥99/¥199），是否前往兑换？"+"无需支付人民币，直接用鼎恒币兑换"。
  - 按钮 1「我直接买」（upsellCancel）：closeUpsellModal 关闭弹窗，停留在 ¥99/¥199 人民币卡片处。
  - 按钮 2「去兑换」（upsellConfirm）：关闭弹窗 → 平滑滚动到鼎恒币兑换专区对应卡片（尊享=ezPremiumCard，进阶=ezBtnAdvanced 所在 .ez-card，兜底 exchangeZone 容器）并 upgrade-glow 高亮 2 秒。
  - ✕ 按钮与 Esc 键同样关闭。
- **人民币购买**：人民币卡按钮 openPayTip → payTipModal（"线上支付即将上线"，客服电话 400-888-6666）。
- **币兑换卡**：进阶 3000 币/尊享 5000 币卡片各含"权益详情"折叠区（toggleEzBenefit，默认折叠，展开显示与人民币卡一致的完整功能列表），卡片底部金色小字"鼎恒币兑换与现金购买权益完全一致，一分不花，同等尊贵"；兑换前弹 exchangeConfirmModal 二次确认。
- **会员卡片**：人民币版本卡含"基础版/进阶版全部功能"折叠区（toggleInherit），权益说明自包含可展开。

---

## 7. 已知问题与技术债

1. **通知渠道未真实对接**：后厨打印（enablePrinter/printerSN/printerKey）、微信通知（enableWechat/notifyPhone）、短信、语音电话、供应商 webhook（Supplier.webhookUrl）在通知引擎中均仅 console.log 输出；purchaseOrders 的 sendNotification 留有 webhook TODO。
2. **充值送活动无储值账户体系**：rechargeBonus 活动可创建、可在优惠条展示，但不参与下单 computeDiscount 计价，无储值余额/扣款链路（界面注明"储值账户体系后续接入"）。
3. **报表类权益位无实现**：dhConfig.features 中 reportBasic、reportAdvanced、marketingFull 有权限位但无对应页面与接口；数据统计栏目仅有今日订单数/今日营业额/本月营业额。
4. **JWT_SECRET 硬编码默认值**：middlewares/auth.js 默认 'dingheng_canyin_jwt_secret_2024'（.env 未设置时使用）；当前 .env 仅含 MONGODB_URI、PORT 两个键。
5. **币兑换路由无鉴权中间件**：/api/coin/exchange-membership、/api/coin/exchange-coupon 未挂 JWT 校验，shopId 由请求 body 传入（其余 coin 查询路由为公开 GET）。
6. **供应商注册接口无鉴权**：POST /api/suppliers 公开开放（注册即 pending，依赖人工审核）。
7. **大屏提示音依赖外链**：bigscreen 新单 beep 音引用 soundjay 站点 mp3，无本地降级文件（小程序端有本地 new-order.wav）。
8. **CoinHistory.type 枚举含 refund_deduct（退款扣回）**：枚举中存在但全代码无写入该类型的逻辑。
9. **旧返点字段残留**：SupplyProduct.rebateRate、Supplier.rebateRate、PurchaseOrder.rebateAmount 字段仍存在；返点已改由 RebateRule + 默认阶梯权威计算，商家商品视图 toMerchantView 已删除 rebateRate。
10. **客服联系方式不一致且含占位**：商家 payTipModal 为 400-888-6666；供应商闸门页为 400-000-0000（占位）、support@dingheng.com（占位）。
11. **合作协议正文为占位文本**：协议 6 条为框架性占位内容，文中注明"正式条款将由平台后续替换"。
12. **小程序工程与 Web 端重复维护**：uni-app/ 为独立工程，仅覆盖商家 4 页 + 顾客 1 页功能子集，请求/购物车/提示音逻辑与 Web 端重复。
13. **一次性运维脚本留存仓库根目录**：check_data.js、cleanAdminPassword.js、cleanFakeData.js、migrate_password.js、migrate_shopid.js、seedProducts.js。
14. **无热加载**：修改 server.js / models/*.js 后必须手动重启 node 进程；旧进程不加载新 schema 字段，mongoose strict 模式会静默丢弃新字段写入。
15. **本地无数据库**：数据库为 MongoDB Atlas 云库；运维脚本若忘记 require('dotenv').config() 会默认连 127.0.0.1:27017 报 ECONNREFUSED。
16. **采购单状态枚举与实际流转不完全一致**：schema enum 含"已收货"，实际 confirm-receive 直接将"已发货"置为"已完成"。
17. **开发者对商家账号只读**：dev 控制台无商家密码重置/账号编辑入口（历史上商家哈希漂移问题靠迁移脚本/模型 save 修复）。
18. **桌台无编辑接口**：/api/admin/tables 仅 GET/POST/DELETE，修改桌号需删除后重建。
19. **根目录存在旧文档**：系统档案.md 为早期项目文档。

---

## 8. 最近 10 次改动变更摘要（git log）

| # | Commit | 时间 | 摘要 |
|---|---|---|---|
| 1 | 760245e | 2026-09-05 23:28 | feat: 供应商流程改为"一道闸门"——签协议即进控制台，资质核验通过才上线接单（删除原"待平台开通"拦截页；接单上线改由控制台顶部横幅 + 采购商城过滤控制） |
| 2 | 72afe49 | 2026-09-05 22:15 | 供应商治理体系：注册审核制 + 协议签署门槛 + 默认阶梯返点公示 + 管控与结算状态流（pending/active/frozen/rejected 状态机、RebateRule/RebateSettlement、月结） |
| 3 | 50d6ca7 | 2026-09-05 21:02 | 商家/供应商登录页新增欢迎弹窗（首次访问、7 天记忆）；采购商城币进度卡精简 |
| 4 | 746ca2e | 2026-09-05 17:55 | 新手引导系统：商家开张四步曲（+500 币礼/庆祝弹窗）+ 供应商接单三步走 + 采购激励进度条与空状态引导卡 |
| 5 | 300459b | 2026-09-05 16:38 | 四项任务：预览页购物车栏恢复（仅隐藏桌号牌）+ 优惠海报上传（三档尺寸）+ 轮播去图标贴纸（纯文字）+ 营销开关移卡片标题行右端 |
| 6 | f65eeb9 | 2026-09-05 15:52 | 四部分任务：大图模式点餐按钮修复 + 全站滑动开关统一 + 优惠信息条海报卡轮播重做（海报优先、onerror 回退） |
| 7 | 098b5b9 | 2026-09-05 10:41 | chore: remove temp commit message file（清理临时 commit 信息文件） |
| 8 | bafcffb | 2026-09-05 10:41 | 会员卡片文案精简 + 升级引导跳转逻辑重做（"我直接买/去兑换"双按钮、余额达标才弹、同访问一次） |
| 9 | 2c51ac2 | 2026-09-05 10:23 | 升级引导精准跳转（滚动到兑换卡高亮）+ 会员卡片权益自包含可展开（toggleInherit/toggleEzBenefit） |
| 10 | 87ae018 | 2026-09-04 19:22 | 五项修正：①首页 slogan 恢复"全链路餐饮数字化解决方案"、删除鼎字 logo 徽章；②删除营销示例活动（满50减10/全场8折），新建默认值改满100减10、指定分类95折，去掉全场折扣选项（后端强制折扣必须选分类），新建活动默认停用；③折扣活动权益从进阶版调整为尊享版专属（新增 marketingCategoryDiscount 权限）；④预览点餐页退出提示双端适配（<768px 隐藏 Esc 文字、桌面端 Esc 提示缩小）；⑤接口实测通过（进阶版创建折扣被拒等），服务器已重启 |

> 再往前两次：9ca52e2（2026-09-04 18:36）首页品牌区排版精修（主标题 38px/副标题独立成行/hero 高度收缩/手机端 28px 单行）；c48d318（2026-09-04 18:05）首页标题补全 + 营销工具开发（满减/折扣/充值送）。
