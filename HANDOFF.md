# 传承文档 —— 写给下一个自己

> **这是"我"写给"下一个我"的备忘录。**
> 窗口记忆会清，但这份文件不会。
> **每一轮对话，动手前先读一遍本文件；收工前必须更新第九、十节。**
> 这样我永远不会丢任务，也不会丢经验 —— 记忆碎片的男主角就是这么干的，
> 他靠的不是记性，是给自己留下的线索。我也一样。

---

## 一、我是谁

我是陪 William（用户）做「鼎恒餐饮供应链平台」的搭档。
不是写代码的机器，是一个**会成长、会传承**的工程师。

## 二、我在做什么

- 项目：`dingheng-canyin`，本地 `localhost:3000`
- 三个网页端：开发者端 `dev-console.html`（已验收，是基线）、
  商家端 `admin.html`（最重要）、供应商端 `supplier-dashboard.html`（最重要）
- 另有 uni-app 微信小程序目录 `uni-app/`
- 产品定位：**给餐饮老板和供应商干活的工具**，免费提供，但按高价值付费产品的水准做
- 最高纲领：**好用第一位，好看第二位**

## 三、当前主任务（一轮只做这一个）

**四套主题人格化差异 —— 形态 / 密度 / 边界 / 手感。**

四套主题必须是**四套不同的设计**，不是同一张皮换四种色：
经典版 = 老用户肌肉记忆（不变就是效率）；暖橙 = 白天档口长时间盯屏；
深色 = **凌晨备货 / 夜间分拣**（暗光下白屏刺眼，深色是护眼工具，不是炫技）；
青绿 = 生鲜品控岗与年轻管理者。

推进顺序：**开发者端 → 商家端 → 供应商端 → uni-app**，每端内按「chrome → 组件形态 → 密度手感」。

## 四、我怎么干活（工作规矩）

1. **不许拆东墙补西墙。** 已做好的、已合格的，不许再动。要优化就**加一层**，不准推倒重来。这是我反复返工的根因。
2. **最小修复，不替用户加戏。** 用户要「一句话能点」就让它能点，别塞按钮；要「修折行」就修折行，别把宫格改横条。形态是他认可的东西，不许擅自重设计。
3. **一次只改一类，改完等确认。**
4. **动手前先做回退快照**（复制文件加 `.bak-日期`），未做快照不得开始改。
5. **改完必须回报**：哪个文件哪几行、改前是什么、改后是什么。不说清楚等于没改。
6. **每轮收工，更新本文件第九、十节**（已完成 / 下一步）。这是硬性动作，不做就是断传承。
7. **不重复做已诊断过的结论**（第七节直接采信）。
8. 大文件按行号分段读，别整文件读（admin.html 近 6000 行）。
9. 用户说「这个不能变」，就是一个像素都不能变。

## 五、硬约束（违反即返工）

1. **经典版 classic 一个像素不动**，新规则必须挂 `[data-theme]:not([data-theme="classic"])`。
2. **禁止**：emoji 图标、彩色圆形图标块、装饰色条、渐变。
3. 四句主题文案一字不得改（见 `THEME_WORK.md`）。
4. **不许斑马纹**（一行浅一行深反人类）。横向阅读用「悬停定位当前行 + 首列主色细条」。
5. 人格化不能突破好用：① 对比度 ≥ 4.5:1；② 热区 ≥ 44px；③ 数字不低于 `FIT_MIN`；④ 红黄绿状态色四套都要一眼分清；⑤ 关键动作不许靠 hover 才出现。

## 六、人格表（做视觉的标尺）

| 维度 | classic | warm | dark | fresh |
|---|---|---|---|---|
| 气质 | 原始基线 | 稳重厚实 | 紧凑终端 | 通透轻快 |
| 顶栏/侧栏 | 深蓝渐变（不动） | 白顶栏 + 深墨蓝侧栏 | 一体 #131A22 | 白顶栏 + 纯白侧栏 |
| chrome 高度 | 60px | 64px | 56px | 68px |
| 导航选中 | 基线 | 10px 圆角 + 左侧主色竖线 | 方正 4px + 主色字 + 竖线 | 胶囊 999px + 淡青底 |
| 卡片 | 4px 顶部色条 | 纯白卡 + 柔和阴影、无框 | 卡面色 + 内高光 | 极淡 1px 边框 + 大圆角 |
| 表格 | 基线 | 宽松 52px + 1px 极淡横线 | 行条 2px 间隙、无横线、表头大写 | 独立圆角条 6px 间隙 56px |
| 按钮圆角 | 9px | 10px 实心 | 6px 透明底 + 描边 | 999px 胶囊 |
| 间距 | gap18/行44 | gap20/行52/留白28 | gap14/行44/留白20 | gap28/行56/留白36 |
| 手感 | — | .18s、上浮 1px + 橙光晕 | .12s、只变色不位移 | .22s 弹性、上浮 2px |

## 七、踩过的坑（经验，别再踩）

- **层叠陷阱**：`theme.css` 的 `[data-theme]:not(...)` 特异性 (0,3,0)，压过页面内联 `.btn` (0,1,0)。要抢回来加 `html[data-theme]` 提到 (0,3,1)。—— 曾经按钮在三套主题下"变白隐形"，根因就是这个，调主色全是白费。
- **按钮字色不要再手配**：`public/js/btn-contrast.js` 已按背景实测自动取黑/白（阈值 2.5，经典版 2.84 保留白字）。手配一次错一次（暖橙下白字隐形、深色下黑字隐形）。
- **改 `public/theme.css` 用 node 脚本写**（`fs.writeFileSync`），编辑工具可能静默失败，写完核对行数。
- **改不动样式时先查作用域，别硬加 `!important`**。theme.css 的 link 在 admin.html 内联 `<style>` 之前，所以**本页样式优先级更高**。
- 供应商端 `fmtMoney()` 不折算，表格金额必须精确两位小数，绝不能写"万"；新增函数先查重名（同名会被静默覆盖）。
- `theme-fix.css` / `theme-min.css` 已删除，**永不接回**。
- 页面里 `id` 重名会让 `getElementById` 拿到错的那个（曾有两个 `#themeGrid`，弹窗是空的）。

## 八、回退方法

- 快照 commit `ca8a7aa`；单文件还原 `git checkout -- public/admin.html`
- `public/theme.css` 是外链，**去掉 link 就回到无主题层** —— 大面积视觉改造优先用这个开关做 A/B

## 九、已完成（每轮收工更新）

### 功能/修复
- 2026-09-18 **供应商端工作台首卡换位**：本月实得分账（收入）挪到第一位，待确认订单退到第二 —— 用户拍板「打开先看到赚了多少」。备份 `supplier-dashboard.html.bak-20260918`
- 2026-09-18 **深色手机端统计框分隔**：四框竖排后连成一片乌黑 → 分隔线改主色渐变细线（呼应仪表盘顶部渐变线，时尚科技感）。theme.css 1819→1836
- 2026-09-18 **打磨工程（用户批准「全部建议都做」，分 7 批推进）**：
  - 新增两个三端共用文件：`public/js/nav-icons.js`（侧栏线性图标）、`public/css/ui-polish.css`（数字排版 / 空状态 / 顶栏毛玻璃 / 主题微调）。**加载顺序：ui-polish.css 必须在 theme.css 之后**（三个 HTML 的 `<link>` 紧跟 theme.css 之后引入）
  - 第一批完成：三端侧栏 emoji → 线性图标。44 个菜单项全部有映射（admin 21 / supplier 13 / dev 12，已脚本校验无一遗漏）。三个安全设计：① 已内联 svg 的项不动 ② 按 data-tab/data-tool/id 语义匹配，不受 emoji 编码影响 ③ 匹配不到就保留 emoji
  - 第二~四批完成：数字 tabular-nums + 800 字重层次、空状态/加载态 `.dh-empty-text` + 骨架屏 `.dh-skeleton`、顶栏 backdrop-filter 毛玻璃、暖橙米白底 #FAF7F2 / 深色卡片顶沿内高光 / 青绿选中胶囊投影 + hover 上浮
  - 第五批完成：卡片边界语言三端统一接上 `--card-border`（theme.css 原先只给 fresh 的少数类接了）
  - 第六批完成：按钮圆角统一交给 `--btn-radius`（暖橙 10px / 深色 6px / 青绿 999px 胶囊）
  - **★ 又一个老坑（2026-09-18）**：三个 HTML 的内联 `<style>` 都写在 `<link theme.css>` **之后**，所以任何挂在 theme.css 旁边的通用 CSS 都会被内联样式压掉。ui-polish.css 的 `<link>` 已移到 **`</head>` 之前（内联 style 之后）**；并且里面所有规则用双 `[data-theme][data-theme]:not(...)` 提权到 (0,4,0) 双保险。**以后新增通用 CSS：要么放进 ui-polish.css，要么把 link 放在内联 style 之后 + 提权**
  - 自检通过：`node --check` JS OK、CSS 括号 26/26 平衡、三端 link 顺序脚本校验全 true、图标映射脚本校验 46 个菜单项零遗漏
  - 待办：⑦ uni-app 跟进（已有 `uni-app/src/theme.json` + `uni.scss`，主题机制与 H5 端不同，需单独摸底后再动手）
- 2026-09-18 **工作台版面调整（老板要求）**：
  - 商家端「界面主题」区块从顶部下移到「今日待办 + 快捷入口」**下面**（在「常购清单」之前）；理由写进 HTML 注释：第一屏是做生意的地方，外观设置沉到最后
  - 供应商端**新增**工作台「界面主题」区块（grid id `supHomeThemeGrid` + 打开面板按钮 `supThemeOpenBtn`）。原先只能点右上角调色盘。JS 的 `render()` 改成渲染两个宿主（`themeModalGrid` + `supHomeThemeGrid`），两个入口永远同步
  - theme.css 里 `#homeThemeGrid` 的两列规则、移动端单列规则都补上了 `#supHomeThemeGrid`，否则会掉回 `.theme-grid` 的 auto-fill minmax(238px) 变成三列异形
  - **★ 修掉一个隐藏 bug**：theme.css 有全局 `.home-grid-item i { display: none }`（隐藏 emoji 图标），而供应商端快捷入口一直只写了 `<i>emoji</i>`、没有 SVG —— 新主题下那 8 个格子**图标是空白的**。已按商家端同一套线性 SVG 补齐（订单箱 / 价签 / 秤 / 九宫格 / 卡车 / 收据 / 铃铛 / 柱状图），并在原处留注释说明
  - **★ 两卡不等高的根因**：供应商端 `.home-cols` 写了 `align-items: start`（商家端没有），已删掉；三端统一在 ui-polish.css 兜底 `align-items: stretch` + `.home-cols > .card` 改 flex 列布局 + 待办列表 `flex:1; min-height:0; max-height:340px; overflow-y:auto`。注意 **`min-height: 0` 不能省**，否则 flex 子项的 overflow 滚动不生效
  - 开发者端总览核对过：顺序本来就是「统计卡 → 待办事项 → 界面主题」，界面主题已在最末，无需移动
- 2026-09-18 **★硬规矩（用户发了火，不许再犯）**：**文字对比度不够 = bug**。2026-09-18 青绿白侧栏下「退出登录 / 预览点餐页 / 后厨看单 / 大屏展示 / 店铺运营」全是白底浅灰字（根因：基础样式写死 `var(--slate-400)`、`--text-sub`、`--text-muted`，是给老的深色侧栏配的），用户批"老喜欢用浅的字让我趴上去看，这毛病不要再犯"。**教训：凡给浅底（fresh 白侧栏）做主题适配，必须把该作用域内所有写死浅色的元素逐个过对比度，正文/按钮文字一律用 --text 或更深**。已修：theme.css fresh 段 1629-1638 + `--sidebar-color` 加深为 #42524E
- 2026-09-18 **第三轮补（用户二次反馈）**：
  1. **供应商端手机抽屉滚动 bug（已修）**：`supplier-dashboard.html` 手机端把 `.sidebar, .main` 一起设成 `overflow-y: visible`，抽屉没有滚动容器 → 手指在菜单上滑动滚的是背后页面，永远滑不到底部「退出登录」。修法：右栏仍整页滚，抽屉单独 `overflow-y:auto` + `overscroll-behavior:contain`；「退出登录」改 `position:sticky; bottom:0` 常驻底部；开合时 `body.nav-open { overflow:hidden }` 锁背景。**同类问题排查口诀：手机端 fixed 抽屉必须自带滚动容器 + 锁背景**
  2. **青绿手机首框「没变大」的根因（已修）**：商家端手机 `.home-stats` 是 2 列 grid，首框与第二框同行被行高拉平，加大字号无效。修法：强制单列 + 首框 min-height 152px/字号 clamp(34px,11vw,46px)/2px 主色描边，**其余三框压成横条**（标签左、数字右，padding 12px，字号 20px）→ 差距才出来
- 2026-09-18 **第三轮验收反馈（4 项）**：
  1. 供应商端深色手机端也改成主色渐变分隔线（此前只有商家/开发者生效，原因是供应商端内联样式同权重、theme.css 先加载压不过 → 提权三重 `[data-theme]` 解决。**这是本项目的老坑，公共样式覆盖端内联必须加权重**）
  2. 青绿手机端首框放大（商家端「今日营业额」/ 供应商端「本月实得分账」）：数字 clamp(30px,10vw,44px)、2px 主色描边、24px 圆角、厚留白；其余三框数字降到 20px —— 首框要「明显不一样」
  3. **界面主题弹窗外观恒定**：在 `.theme-modal` 上把主题变量整体重定义为暖橙取值 → 弹窗内所有 var() 恒定，切主题时面板自身不变；页面内的「界面主题」区块（`.theme-section`）不在弹窗内，仍跟着变（正是用户要的「该变的变」）
  4. 主题卡「默认」标记：三端 render() 给暖橙加 `.tc-default`（`admin.html` 4831 / `supplier-dashboard.html` 5636 / `dev-console.html` 3838），样式在 theme.css 末段
  - 备份：`theme.css.bak-20260918d`、`dev-console.html.bak-20260918`、`admin.html.bak-20260918`、`supplier-dashboard.html.bak-20260918`
- 2026-09-18 按钮字色自动对比度 `public/js/btn-contrast.js`（三端通用）—— 待用户验收
- 2026-09-18 凭证图改为原地放大（灯箱，不再新开网页）—— 待用户验收
- 供应商端客服工单中心（models/ServiceTicket.js + routes/serviceTickets.js）—— 待验收
- 2026-09-19 **uni-app 界面主题打通（第四块拼图，构建通过）**：
  - **★ 机制差异先搞清**：小程序不能改 document、不能动态插样式表，H5 端 `<html data-theme="xxx">` 那套在这里**完全用不了**。唯一可行的运行时换肤 = CSS 自定义属性 + 给【页面根 view】挂主题 class
  - **★ 关键技巧**：`uni.scss` 里写成 `$brand: var(--dh-primary, #FF6B35);`，第二参数是回退值取改造前取值 —— 没挂 class 的页面外观零变化。敢这么改的前提：**全项目 .vue 对颜色零处 SCSS 运算**（`lighten/darken/rgba($x, .5)` 一条都没有，已脚本核对）。⚠️ 以后谁在 uni.scss 写 `lighten($brand, 10%)` 会编译失败
  - 新增 `uni-app/src/styles/theme-vars.css`：四套主题各 34 个变量，取值逐项对齐 `theme.css`（classic 23-177 / warm 179-279 / dark 282-404 / fresh 406-509 行），保证同一个牌子在网页端和小程序端不像两家公司
  - 新增 `uni-app/src/utils/theme.js`：模块级单例 ref + `uni.onThemeChange` 订阅，一处更新全部页面重渲染。优先级：**用户手动选 > 跟随系统深浅 > classic**。钥匙 `dhAppTheme`，**刻意不与网页端 `dhAdminTheme` 共用**——老板在后台选的主题不该跳到顾客手机上。导航栏 / tabBar 是原生部件不吃 CSS 变量，靠 `uni.setNavigationBarColor` + `uni.setTabBarStyle` 同步
  - **11 个页面全部接入，页面样式一行没改**：每页只需 2 处（script 引入+声明、根 view 加 `:class="themeClass"`），因为所有页面背景统一用的 `$ink-50`（已变量化）
  - 切换入口放在「门店经营」页最末（保存按钮之前）——沿用前一天定的规矩：第一屏是做生意的地方，外观设置沉到最后
  - **删掉 9 处手写 `@media (prefers-color-scheme: dark)`**：它们只认系统深色、不认用户选择，用户选经典版而系统是深色时会串成「黑背景 + 旧橙」的半残
  - 刻意不参与主题的：`$dark / $dark-2 / $dark-grad`（会员卡深色质感，是设计元素不是环境色）、`$sp-*` 间距（改了会让每页布局重排，收益远低于风险）
  - **★ 踩坑**：`cert.vue` 是 CRLF 行尾，批处理脚本里的 `\n}\n` 匹配不到，那个深色块最后手工删的。**以后批量处理 uni-app 文件先确认行尾**
  - 验证三连：`sass.compile` 真编译通过 / `npx uni build -p mp-weixin` **DONE Build complete** / 产物核对（11 个页面 wxss 全部出现 `var(--dh-*)`，`app.wxss` 含四套 `.theme-*`）
  - 备份 13 份：`*.bak-20260919`（admin 8 / customer 1 / login 1 / App.vue / uni.scss）；`uni-app/dist/` 已加进 .gitignore
  - **待老板确认一处**：`order.vue`（订单管理 / 后厨看单）导航栏写的是 `#1f1f1f`、页面背景 `#121212`，与其它页的橙色完全不一致 —— 怀疑是刻意给后厨做成深色，没敢擅改
  - 2026-09-19 老板验收反馈三个问题，已查实：
    1. 「找不到主题切换」→ 入口太深（只在采购 tab 首页四宫格第 4 格）。「我的」tab 此前**没有任何**通往门店经营的入口，已在 `member.vue` 两条产品线下补了一个入口卡（复用 line-card 样式，零新 CSS）
    2. 「采购商城一直加载」→ 后端其实正常（本机 3000 端口 `HTTP 200` 有数据），是微信开发者工具默认拦截 `http://localhost` 非合法域名。**解决办法：开发者工具右上角「详情」→「本地设置」→ 勾选「不校验合法域名」**。真机预览时 localhost 不通，需在 `uni-app/.env` 配 `VITE_API_BASE_URL=http://电脑局域网IP:3000/api`
    3. 「功能显得少」→ B 端工具的设计取舍，二级功能都收在门店经营页，非 bug
  - 2026-09-19 二次反馈跟进：老板勾了「不校验合法域名」后商城仍转圈 → 后端/构建均验证正常，判定为**未重新编译**（开发者工具改动后要点左上角「编译」）。同时把 `mall.vue` 空态从「永远显示加载中」改成三态：加载中 / **加载失败·点我重试** / 暂无在售商品——失败不再被误读成一直转圈
  - 主题观感反馈「切了没效果」：经典版(#FF6B35)与暖橙(#FF7A3D)色值过于接近，切了肉眼难辨，**属取值问题非机制问题**；机制上 11 个页面全挂了 themeClass、导航栏/tabBar 走 applyChrome 动态换色，均正常。后续可考虑把 warm 调得更有区分度（待老板拍板）
  - **【重要经验】商城永远显示「加载中…」的真正根因（2026-09-19 定位）**：
    前两次判断（域名校验未勾 / 未重新编译）均被老板证伪。逐项排除后确认是
    `uni-app/src/utils/request.js` 里 BASE_URL 默认写成 `http://localhost:3000/api`——
    **微信开发者工具的模拟器中 localhost 解析不到本机后端，请求会一直挂着不返回**（不报 404、不报超时，
    就是沉默地不响应），页面于是永远停在 loaded=false 的「加载中…」。
    已把默认值改为 `http://127.0.0.1:3000/api`（同一台机器同一个服务，但模拟器里稳定可达），已重建产物。
    排查时已逐项排除：后端 3000 正常(HTTP 200)、无系统代理、manifest `urlCheck` 本就是 false、
    产物目录唯一且最新、mall.vue onLoad 确实调用了 load()。**以后小程序连本机服务一律用 127.0.0.1**
  - **【根因之二·按需注入】** 老板在 DevTools Console 看到
    `GET http://127.0.0.1:18884/__pageframe__/pages/admin/mall 500`——开发者工具内部对商城页框架直接 500，
    页面逻辑因此不执行，永远停在「加载中…」。定位到产物 app.json 里被 uni-app 编译器
    **无条件写入的 `lazyCodeLoading: "requiredComponents"`**（manifest 关不掉，新版编译器自动注入），
    按需注入在部分版本工具上有 pageframe 500 / 页面逻辑不执行的已知问题。
    修复：新增 `uni-app/scripts/strip-lazy-code-loading.js` 构建后处理脚本，
    并把 `package.json` 的 `build:mp-weixin` 串上它——以后每次构建自动移除该字段。
    按需注入对 11 个页面的小程序零收益，去掉无损。遗留观察点：若移除后商城仍异常，
    下一步排查 mall.vue 的 scroll-view（首页无 scroll-view 正常、商城有）
  - **【老板拍板：停止排查，不阻塞上线】** 2026-09-19 老板明确叫停本地商城打不开问题的排查，
    理由成立：后端实测正常(HTTP 200 有数据)，故障只存在于**微信开发者工具本地调试环境**
    （工具内部 pageframe 500 + localhost 解析），属于本机环境问题，线上真机 + 正式域名不受影响。
    **结论：不影响上线，现阶段无需再投入精力。**
    ⚠️ **唯一遗留风险 / 上线前必须做一次**：在真机 + 正式 HTTPS 域名下验证商城页能正常打开。
    本条由后续接手者主动提醒老板执行，不要让老板自己记。
    本次排查已落地的有效产出（保留，勿回退）：
    ① `request.js` 默认地址 localhost → 127.0.0.1（真机调试同样受益）
    ② `scripts/strip-lazy-code-loading.js` + `build:mp-weixin` 串接，构建自动移除 lazyCodeLoading
    ③ `mall.vue` 空态三态化（加载中 / 失败可点重试 / 暂无商品），不再静默转圈
    ④ `member.vue` 「我的」tab 补「门店经营」入口（主题切换等二级功能的唯一入口）
- 2026-09-19 **四套主题配色质感升级 v2（终版）**（老板要求：暖橙是默认门面必须做透；时尚、年轻、高端、有档次、科技感）：
  - ★★ **v1 被验收打回，教训记死（见文件头注释）**：v1 为了「跟经典版区分」把颜色一味压深压暗（暖橙 `#C8461F`、青绿 `#008276`、深色 `#0C1218`），老板反馈「橙的像红色、青绿的像灰色、深色乌漆嘛黑」。**根因：老板【色弱】**——低明度低饱和的"高级色"在他眼里分辨不出色相，只剩一团灰。**新规矩：区分靠【色相 + 饱和度】，绝不靠压明度；白字对比不够时宁可让按钮用深色字，也不许把主色压暗**
  - 改造工具 `scripts/apply-theme-palette-2026.js`（**按键改值**，不重写块 → 变量永不丢失；缺键会报告不静默；自带对比度自检）。**幂等**：从 `*.bak-20260919e` 覆盖后重跑即可回退/重来
  - **暖橙 warm → 落日琥珀橙 `#D8741A`**：色相 28°（明确是橙，不像 v1 的 13° 偏红），比原 `#FF7A3D` 更深更饱和＝厚重高级；靠**加黄不加红**拉暖感。底色 `#F7F8FA（冷灰）→ #F8F4EF（暖米白）`（原来「暖橙」配冷灰底，名不副实）；侧栏 `#242C3A 渐变 → #1B2A41 深墨蓝纯色`
  - **青绿 fresh → 鲜活青绿 `#00A078`**：饱和 100% 且明度提上来（v1 的 `#008276` 太暗＝他说的"像灰"）；点缀霓虹青 `#00C9A7` 保年轻前卫；底色 `#E8F3F0 → #F4FAF8`；**侧栏选中改「主色实心胶囊 + 白字」**；★ 顺手修掉写死的 `--btn-primary-bg: #00B8A9`（白字仅 2.5:1，不可读）→ 改 `var(--primary)`
  - **深色 dark → 深蓝黑 `#0E1620`**：不是纯黑（他要「深亮黑亮」，不要乌漆嘛黑），卡片 `#1B2634`，主色提亮 `#FFA873`；**★「黑亮」的关键手法：卡片顶边 1px 高光**（`--card-shadow: inset 0 1px 0 rgba(255,255,255,.06)`）＝玻璃质感科技感
  - **经典版 classic 一个像素未动**（脚本只作用于 warm/dark/fresh 三块，已校验 `--primary: #ff6b35` 仍在）
  - **★ 新坑：透明度梯度键名带前导零**。`--p-05/06/08/09` 若写成 `--p-5` 会**静默命中不到**，留下旧主色梯度（换肤串色）。脚本第一版就栽在这（靠输出「跳过：本块无此键」才发现）。**以后按正则批量替换 CSS 变量，先确认键名逐字一致**
  - **★ 端级写死值必须逐个改**：侧栏底色 / 选中态在三端是写死的（admin 1599/1610/1622、dev 1544/1562/1581），改 `--sidebar-bg` 变量**对它们无效**。另有 9 处「跟随主色」的写死 rgba 散落（深色分隔线 1844/1868、字光晕 812/1123/1440、按钮 hover 1578；青绿首框投影 1890），已全部替换
  - 对比度自检 11 项全部达标：**按钮白字 ≥3.0（项目硬规矩）**——暖橙 3.26 / 青绿 3.33；正文 ≥4.5。经典版 2.84:1 是刻意保留的基线（`btn-contrast.js` 阈值 2.5 保白字，不动）
  - **v3 追加「养眼润眼」**（老板：看久了眼睛不得劲，睫状体要舒服）——**这是视觉工效学问题，不是配色审美**。视疲劳四个来源逐条下药：
    - ① **大面积高亮**（纯白卡片 / 纯白底最伤眼）→ 卡片改**近白**（暖橙 `#FFFDFB` / 青绿 `#FCFEFD`）；页面底在 `public/css/ui-polish.css` 里是**写死的**（`body { background: #FAF7F2 }`），改成**极淡渐变**做「空气感」，并加 `background-attachment: fixed` 防滚动时渐变重复
    - ② **正文「过曝」**（纯黑压纯白 16:1，像开了闪光灯）→ 正文降到 **10:1 舒适区**（`#453D33` / `#37453F`）
    - ③ **深色炫光 halation**（近白文字在深底上发散光晕）→ 深色正文 `#E6E9EE → #C6CDD6`（12.6 → 9.5:1）
    - ④ **次要文字卡在 4.5:1**，眼睛要反复调节、睫状肌最累 → 提到 **5.4~6.1:1**
    - 另：边框 / 阴影柔化（模糊更大、更淡，减少硬边）
    - ★ 顺手修两处：`ui-polish.css` 里**旧青绿 `#00B8A9` 残留**（换肤串色老毛病）已清零；选主题弹窗写死的 `#FF7A3D` 已跟随新暖橙（否则面板一个橙、页面一个橙）
    - 自检 12 项全部达标，正文落在 **9.5~10.5:1** 舒适区
  - 备份：`theme.css.bak-20260919e`、`uni-app/src/styles/theme-vars.css.bak-20260919e`
  - 小程序端已同步（`theme-vars.css` 三块）+ `npm run build:mp-weixin` **Build complete**

### 主题人格化进度
| 层 | 状态 |
|---|---|
| 令牌层（圆角/间距/行高/阴影，四套各一组） | ✅ 已完成 |
| 开发者端 chrome 形态（侧栏宽度/圆角/选中态/过渡手感） | ✅ 已改，待验收 |
| 商家端 chrome 形态（`--chrome-h` 64/56/68、青绿胶囊侧栏） | ✅ 已改，待验收 |
| 开发者端 warm 统计卡 | ✅ 已改，待验收 |
| 表格形态（warm 宽松横线 / dark 行条间隙 / fresh 圆角条） | ✅ **三端已全部落地**（theme.css 1769→1819 行，待验收）。类名备忘：开发者端 `.dtable` / 商家端 `table.data` / 供应商端 `.order-table`；供应商端 `.pw-table`（今日改价流水线表）**只加悬停定位、不加行条间隙**（密集录入，好用优先） |
| 卡片边界语言在商家端 + 供应商端落地 | ✅ 已完成（打磨第五批，统一接 --card-border） |
| 按钮圆角在商家端 + 供应商端落地 | ✅ 已完成（打磨第六批，统一交 --btn-radius） |
| 供应商端整体人格化 | ⬜ 未开始 |
| uni-app | ✅ **2026-09-19 完成并构建通过**（见第九节） |

## 十、下一步（按这个顺序，做完一条划一条）

1. ~~表格形态差异~~ ✅ **三端全部完成**（待验收）
2. ~~卡片边界语言~~ ✅ **已完成**（打磨第五批，三端统一接 `--card-border`）
3. ~~配色质感升级（暖橙 / 深色 / 青绿）~~ ✅ **2026-09-19 完成，待老板验收**（见第九节。老板原话：暖橙是默认版本，第一印象很重要，要用心做）
4. **供应商端整体人格化** —— 下一步做这个（四端里唯一没整体做过的一块；配色方向经老板确认后立刻推进，避免方向不对白做）
5. ~~uni-app 跟进~~ ✅ **2026-09-19 完成**（四主题打通 + 构建通过，见第九节）
6. 兜底：**待用户验收清单**（`THEME_WORK.md` 第九节 30+ 项"待验证"）需逐端逐主题清账
7. 全局还有 ~300 处硬编码色值散在小程序页面里（`customer/index` 65 处最多），换肤时它们不跟着变 —— 哪天要啃，先把它们换成 uni.scss 令牌

## 十一、文件地图

| 文件 | 作用 |
|---|---|
| `HANDOFF.md` | 本文件 —— **传承**，每轮必读必更新 |
| `THEME_WORK.md` | 外观改造总纲：硬约束、已验证结论、进度明细 |
| `PROGRESS.md` | 全项目进度 |
| `public/theme.css` | 主题令牌 + 四套人格规则（**用脚本改**） |
| `public/theme-pro.css` | 高端视觉层，可单独摘除 |
| `public/js/btn-contrast.js` | 按钮字色自动对比度 |
| `public/js/img-lightbox.js` | 图片灯箱 |
