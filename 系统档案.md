# 系统档案

## 一、概况
- Node.js+Express+MongoDB(Mongoose)+JWT；前端 public/ 纯 HTML/JS 多页（index.html 为三端入口），另有 uni-app 小程序工程
- 定时任务：每日 02:00 币过期/会员降级/券过期；每月 1 号 03:00 供应商返点月结

## 二、功能完成度清单
**顾客端**：✅扫码选店选桌/分类浏览/购物车/下单（H5+小程序，小程序支持离线缓存）；❌下单后无订单状态跟踪；⬜微信支付（小程序仅留 payOrder 死代码，后端无支付接口）；⬜顾客账号/会员价/积分/券/签到分享
**商家端**：✅注册登录、菜单与分类增删改、桌台、订单列表与完成、后厨语音接单、大屏展示；✅鼎恒币中心（余额/流水/兑月卡/兑采购券）；✅采购商城（逛店/加购/起送校验/得币徽章）与采购订单全流程（下单→用券→确认→发货→收货自动发币）；🟡数据统计仅今日 4 个数字；🟡打印/微信通知只有开关无真实通道；🟡顾客积分表单只存 localStorage 不生效；⬜满减/折扣/充值送；⬜现金买会员/权益对比页
**供应商端**：✅注册登录、新单弹窗语音、确认/发货、单笔预估与实际返点、统计、通知开关；🟡无返点余额与月结单汇总页，短信/语音开关不接真实通道；❌商品管理页为"开发中"占位（API 已备但无 UI，商品只能脚本/接口录入）
**开发者端**：✅登录、总览统计、商家/供应商列表与详情、对账中心、返点规则配置、返点概览与月度结算、得币倍率配置；❌系统设置为"开发中"占位页

## 三、已知问题
- admin.js 硬编码前端管理密码 admin123（遗留密码门可绕过，真鉴权为 JWT）；Setting.adminPassword 默认 admin123 未实际使用
- 两处"开发中"占位页：供应商商品管理、开发者系统设置
- 顾客积分设置存 localStorage：不进库、不参与计算、换设备丢失
- 打印/微信/短信/语音通知均未接真实通道，仅开关+console 日志
- 小程序 BASE_URL 写死 localhost:3000；微信支付无后端下单接口
- 会员宣传的经营报表/顾客画像/损耗分析/高级营销均无实现，dhConfig 仅留权限位
- 三端改密接口已上线，但各后台均无"修改密码"按钮入口
- 采购券只能鼎恒币兑换产出，无手动发券；顾客侧无任何券
- server.js 自动建 Setting 处有无效 shopName 兜底表达式（小瑕疵）

## 四、商业功能缺口
- 商家营销（满减/折扣/充值送）：⬜全缺，仅 dhConfig 留 premium 权限位
- 会员销售页（展示会员价+现金购 99/199 月卡）：⬜无，现仅支持鼎恒币兑换
- 会员权益对比页：⬜无独立页，权益仅以锁定提示分散出现
- 顾客积分玩法（签到/分享/裂变/等级）：⬜全缺，顾客无账号体系
- 优惠券发放：⬜无发券/领券中心，券仅币兑且仅限采购抵扣

## 五、数据集合
Admin；Category/Dish/Table/Order/Setting（均按 shopId 隔离）；ShopAccount（商家账号/pointsBalance）；Member（memberLevel/memberExpire/dinghengCoin，注册即送进阶版 30 天）；CoinHistory（FIFO 批次：amount/remaining/expireAt/balanceAfter/type）；CoinRule（category 唯一/multiplier 默认 1/enabled/remark）；Coupon（采购抵用券：faceValue/minOrder/status/expireDate）；Supplier（loginAccount/rebateRate/balance/notificationSettings）；SupplyProduct（costPrice/marketPrice/stock，商家视图已剔除 rebateRate）；PurchaseOrder（items 品类快照/totalAmount/actualPayAmount/preRebate/actualRebateAmount/rebateSettled/rewardCoin/appliedCouponId）；RebateRule（supplierId+category+tiers 档位）；RebateSettlement（月结单）

## 六、会员三档（dhConfig）
- 基础版免费：返币 2元=1币，兑 10/20/30 元券
- 进阶版 99元/月（3000币）：1元=1币，解锁顾客积分/经营报表/50/100 券；注册首月赠送
- 尊享版 199元/月（5000币）：1元=1币，解锁高级营销/顾客画像/200 券

## 七、鼎恒币
- 发币：采购确认收货按明细逐行计算 = 行金额×会员返币率×品类倍率（CoinRule 配置，默认 1 倍，未配置走"其他"兜底再默认 1），汇总向下取整；流水类型 purchase_reward，含加成附注"含品类倍率加成"
- 90 天有效、FIFO 扣减、每日定时清过期；可兑月卡（3000/5000 币）与采购券（券 30 天有效）
- 倍率仅开发者可配即时生效；商家商城只见"该品类 X 倍得币"徽章，看不到返点率

## 八、返点
- RebateRule 按供应商+品类设月累计档位（费率/全额或超额模式），无规则兜底 8%；订单确认时预估 preRebate，月结生成 RebateSettlement、回写 actualRebateAmount、计入 Supplier.balance（幂等）

## 九、API 路由
- 公共：GET categories/dishes/tables/orders/settings/admin-stats；POST orders；PUT orders/:id/complete
- 商家：admin/categories、admin/dishes、admin/tables 增删改查；GET admin/orders、admin/merchant-stats；PUT settings
- 认证：merchant/supplier/dev 的 register、login；dev/seed；三端 change-password
- 开发者：dev/stats、merchants(/:id)、suppliers、reconciliation、rebate/overview、rebate/settlements、rebate/rules(GET/POST，PUT/DELETE /:id)、rebate/settle、coin-rules(GET/POST)
- 供应链：purchase-orders（下单/confirm/deliver/ship/confirm-receive/receive/apply-coupon、supplier 视图）、supply-products(CRUD)、supplier/orders/pending、supplier/stats、supplier/settings
- 鼎恒币：coin/exchange-membership、exchange-coupon、status/:shopId、history/:shopId、category-multipliers、member/permissions/:shopId
